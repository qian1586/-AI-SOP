# ================================================================
# 只提交"人和机器都需要的东西"：源码、脚本、文档、配置样例。
# 不提交：编译中间产物（bin/obj，每次重建都变）、日志、运行数据、临时文件。
# 这样仓库一直很小、diff 也干净，`git log` 一眼能看出改了什么功能。
# ================================================================

# 编译输出（可随时重建）
bin/
obj/
*.user
*.suo

# 运行期数据（每个工位自己产生的，不属于代码）
data/
*.log
logs/

# 自动生成的中间/备份文件
*.tmp
*.bak
*.bad-*
*_wpftmp.csproj

# 上传脚本生成的版本号文件保留，但要忽略它的临时副本
.version.tmp

# 上传用的 GitHub 令牌（绝对不能进仓库）
.github-token

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store
