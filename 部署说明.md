# VisionForge 部署说明（Windows 工控机 / 本机）

> 版本 v1.0.0 · 2026-09-11
> 配套文档：`app\现场部署接线手册.md`（硬件接线、相机触发配置、PLC 地址、ROI 标定、故障排查）

---

## 一、这个包是什么

`VisionForge.Main.exe` 是 **框架依赖** 发布包（体积小、升级只换 dll）。
代价是：目标机必须先装 **.NET 8 桌面运行时**，否则双击 exe 只会弹一句英文报错：

```
You must install or update .NET to run this application.
Framework: 'Microsoft.WindowsDesktop.App', version '8.0.0'
```

本包里的几个脚本就是围绕这一条来的 —— 普通操作员不用敲命令，双击即可。

```
VisionForge-部署包-v1.0.0\
├─ 0-一键部署.cmd                ← 最省事：装运行时 + 装程序 + 建桌面快捷方式，一次搞定
├─ 1-安装.NET8桌面运行时.cmd     ← 第 1 步：补齐运行时（缺了程序起不来）
├─ 2-环境自检.cmd                ← 第 2 步：一次性体检（系统/运行时/文件/权限/相机/PLC）
├─ 3-启动VisionForge.cmd         ← 第 3 步：启动程序
├─ 4-部署到D盘.cmd               ← 可选：整包装到 D:\VisionForge 并建桌面快捷方式
├─ 5-修复并重启.cmd              ← 缺陷修复：用源码重新编译并覆盖已部署程序（见第九节）
├─ 部署说明.md                   ← 本文件
├─ scripts\                      ← 上面几个 cmd 实际调用的 PowerShell 脚本
├─ src\                          ← 完整源码（5 个工程 + Halcon 文件），修复重建用
└─ app\                          ← 程序本体
   ├─ VisionForge.Main.exe
   ├─ VisionForge.*.dll
   ├─ VisionForge.Main.deps.json / .runtimeconfig.json
   ├─ config\appsettings.json    ← 相机 / PLC / 报警地址配置
   └─ 现场部署接线手册.md
```

> 程序运行会在 `app\` 同级自动创建 `data\`（配方、SOP、历史 JSONL、NG 图、日志）。
> 所以 **整个包要放在可写目录**（如 `D:\VisionForge\`），不要放 `C:\Program Files\` 下。

---

## 二、最省事的方式：双击一次

双击 **`0-一键部署.cmd`**，它会一口气做完：

1. 装 .NET 8 桌面运行时 —— 有管理员权限就装机器级（弹一次 UAC，点「是」）；
   权限不够就自动改用当前用户级安装，全程不弹 UAC。
2. 把程序复制到 `D:\VisionForge`（该目录不可写时自动退到
   `%LOCALAPPDATA%\VisionForge`）。
3. 在桌面建一个「VisionForge」快捷方式 —— 以后双击桌面图标就能启动。

整个过程大约两三分钟，窗口里会一行行打印进度，全在 `%TEMP%\visionforge-setup.log` 留痕。
装完桌面会出现「VisionForge」图标，双击即用。

> 需要指定别的目录：`powershell -ExecutionPolicy Bypass -File scripts\one-click-setup.ps1 -Target "E:\VisionForge"`

---

## 三、分步部署（想一步一步确认时用）

### 第 1 步：安装 .NET 8 桌面运行时

双击 **`1-安装.NET8桌面运行时.cmd`**。

- 先自动检测：已经装好就直接显示版本号并退出，不会重复装。
- 没装则自动下载官方安装包（约 60MB）→ 校验数字签名 → 弹一次 UAC → 静默安装。
- 拿不到管理员权限时，脚本会自动退化成 **当前用户级安装**（装到
  `%LOCALAPPDATA%\VisionForge\.dotnet`，并写入用户变量 `DOTNET_ROOT_X64`），
  不需要管理员也能跑起来。要直接走这条路，命令后加 `-UserScope`。

### 第 2 步：环境自检

双击 **`2-环境自检.cmd`**，会依次打印：

| 检查项 | 说明 |
|---|---|
| 操作系统 | 必须是 64 位 Win10/11（本包只有 win-x64） |
| .NET 运行时 | 重点看有没有 `Microsoft.WindowsDesktop.App 8.x` |
| 程序文件 | exe、4 个 dll、`config\appsettings.json` 是否齐全 |
| 目录权限 | 程序目录必须可写（要建 `data\`） |
| 相机 / PLC | 配置里不是 Mock 时，会 ping 相机、测 PLC 端口通不通 |

结论为「全部检查通过」就可以进第 3 步。

### 第 3 步：启动

双击 **`3-启动VisionForge.cmd`**。

- 运行时缺失时，它会用中文说清楚并问你要不要当场安装，不会再甩英文报错。
- 启动后先看日志 `app\data\logs\`，再点界面上的「连接相机」→「执行检测」。
- 默认配置是 **模拟相机 + 模拟 PLC**，不接任何硬件就能把 SOP 全流程跑通 ——
  **建议先用这个模式验证界面和判定逻辑，再切真实硬件。**

### 第 4 步（可选）：整包装到 D 盘

双击 **`4-部署到D盘.cmd`**，会把整个包复制到 `D:\VisionForge`（可在命令行用
`-Target "E:\VisionForge"` 改路径），并自动建一个桌面快捷方式「VisionForge」。
以后现场操作员双击桌面图标就行，不用再去翻目录。

- 只复制程序，不动已有的 `data\`，所以升级版本时不会覆盖配方和历史。
- 目标目录没权限时会自动提权一次；不想建快捷方式加 `-NoShortcut`。

---

## 四、本机（当前这台）实测状态 · 2026-09-11

| 项目 | 结果 |
|---|---|
| 发布包完整性 | 通过。exe + 4 个 dll + deps/runtimeconfig + config 齐全，dll 可正常读取程序集元数据，`appsettings.json` 解析正常 |
| 操作系统 | Windows x64 |
| 已装运行时 | `Microsoft.NETCore.App 6.0.8 / 8.0.21`、`Microsoft.WindowsDesktop.App **6.0.8（仅此一个）**` |
| 缺什么 | **`Microsoft.WindowsDesktop.App 8.x`** ← 唯一的阻塞项 |
| 直接双击 exe | 失败，报 `You must install or update .NET to run this application.`（已复现） |

补上运行时后即可启动，命令与界面部分无需任何改动 —— 双击 `0-一键部署.cmd` 就是补这一步。

---

## 五、目标机没有外网时（离线安装）

内网工控机很常见。在有网的电脑上下载这两个文件，拷到工控机：

| 文件 | 用途 | 官方地址 |
|---|---|---|
| `windowsdesktop-runtime-8.0.21-win-x64.exe`（约 60MB） | 装运行时，**必下** | https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/8.0.21/windowsdesktop-runtime-8.0.21-win-x64.exe |
| `dotnet-install.ps1`（约 50KB） | 仅在「没有管理员权限」时需要 | https://dot.net/v1/dotnet-install.ps1 |

**有管理员权限**：双击那个 exe，一路下一步，然后跑 `2-环境自检.cmd` 确认。

**没有管理员权限**（用户级安装，不需要 UAC）：

```powershell
powershell -ExecutionPolicy Bypass -File dotnet-install.ps1 -Channel 8.0 -Runtime dotnet -InstallDir "$env:LOCALAPPDATA\VisionForge\.dotnet" -NoPath
powershell -ExecutionPolicy Bypass -File dotnet-install.ps1 -Channel 8.0 -Runtime windowsdesktop -InstallDir "$env:LOCALAPPDATA\VisionForge\.dotnet" -NoPath
```

（两条都要跑：第一条装控制台运行时，第二条才是 WPF 桌面运行时。）
装完把用户环境变量 `DOTNET_ROOT_X64` 指向该目录，重启资源管理器或重开窗口生效；
用 `3-启动VisionForge.cmd` 启动则会自动补上这个变量。

---

## 六、切到真实硬件

改 `app\config\appsettings.json` 后重启程序：

```jsonc
{
  "Cameras": [
    { "Id": "CAM-001", "DisplayName": "工位1 · HC-500-10GM",
      "Vendor": "GigE",                       // Mock → GigE
      "Model": "HC-500-10GM", "SerialNumber": "相机序列号" }
  ],
  "Plc": { "Brand": "ModbusTcp",              // Mock → ModbusTcp
           "IpAddress": "192.168.1.10", "Port": 502 }
}
```

切真机前请先按 `app\现场部署接线手册.md` 完成：相机硬触发（Line0 / 上升沿）设置、
光电接线、三色灯与蜂鸣器地址（M110~M113）、ROI 标定。切换后建议先跑一次
`2-环境自检.cmd`，确认相机和 PLC 都通。

---

## 七、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 双击 exe 弹英文报错 | 没装 .NET 8 桌面运行时 | 跑 `1-安装.NET8桌面运行时.cmd` |
| 脚本提示「上不了外网」 | 工控机离线 | 按第五节离线拷贝安装包 |
| 安装脚本下载慢/被杀软拦截 | 企业策略 | 改用离线安装包，或加 `-UserScope` 走用户级安装 |
| 启动后没有任何反应 | 目录不可写、data 建不出来 | 把包移到 `D:\VisionForge\` 之类的可写盘 |
| 程序打不开、闪一下就退 | 缺 dll 或被杀软隔离 | 重新完整拷包；看 `data\logs\` 日志 |
| 双击后进程在跑但**没有窗口**、日志停在「数据目录」那行 | v1.0.0 的启动死锁（见第九节） | 双击 `5-修复并重启.cmd` |
| 一直「等待触发」 | 相机没配硬触发、光电没接 Line0 | 见手册第 3 节与第 7 节 |

---

## 八、部署检查清单

- [ ] 运行时检查通过（`Microsoft.WindowsDesktop.App 8.x`）
- [ ] `2-环境自检.cmd` 全部通过
- [ ] Mock 模式跑通全流程（连接相机 → 执行检测 → 判定 → 历史记录）
- [ ] 切真实相机后能出图、硬触发能拍一张
- [ ] ROI 标定并保存配方
- [ ] SOP 步骤顺序与工序一致
- [ ] NG 时锁线 + 红灯 + 蜂鸣 + 存图
- [ ] 历史查询与 CSV 导出正常

---

## 九、缺陷修复记录（v1.0.1 · 2026-09-11）

现场部署时实测发现：**v1.0.0 编译出来的程序启动后界面永远出不来**。定位过程与结论如下。

### 现象

双击启动后进程活着（任务管理器可见 `VisionForge.Main.exe`），但：

- 没有任何窗口（窗口句柄为 0）；
- 日志只写到第 2 行就停：`========== VisionForge 启动 ==========` / `数据目录：...`；
- `data\recipes\` 里留下一个没来得及改名的 `*.json.tmp`；
- 8 个线程全部处于 Wait，运行 3 分钟只消耗 0.28 秒 CPU —— 典型的阻塞等待，不是卡在计算。

### 根因

三个位置在 UI 线程上**同步等待异步调用**，而整个项目的库代码一处 `ConfigureAwait(false)` 都没有：

| 位置 | 代码 | 触发时机 |
|---|---|---|
| `App.xaml.cs` 示例配方落盘 | `recipes.SaveAsync(recipe).GetAwaiter().GetResult()` | **首次启动必现** |
| `MockCamera.Dispose` | `StopGrabbingAsync().GetAwaiter().GetResult()` | 关闭窗口时 |
| `MockCamera.DisconnectAsync` | 同步方法里等 `StopGrabbingAsync()` | 断开相机时 |

`SaveAsync` 里的 `await File.WriteAllTextAsync(...)` 会把续体投递回当前的
SynchronizationContext（即 UI 线程），而 UI 线程正卡在 `GetResult()` 上等它 ——
续体永远排不上队，这就是教科书式的**异步死锁**。`.tmp` 文件写出来了却没被
`File.Move` 改名，正好卡在第一个 await 之后，与现象完全吻合。

### 修复

1. `App.xaml.cs`：改用 `Task.Run(() => recipes.SaveAsync(recipe)).GetAwaiter().GetResult()`，
   让写入在线程池上完成，不再依赖被阻塞的 UI 线程。
2. `JsonRecipeRepository`：三处 await 全部加 `ConfigureAwait(false)`（库代码不该绑定调用方的上下文）。
3. `MockCamera.DisconnectAsync`：改成真正的 `async` 方法；`Dispose` 改用 `Task.Run(...)` 等待。
4. `HikCamera`：同类写法一并改掉（该文件默认不参与编译，启用海康 SDK 后才生效）。

### 复现与验证

修复前：启动 → 界面不出、日志 2 行、留下 `.tmp`、进程卡死（已实测复现）。
修复后：`5-修复并重启.cmd` 会重新编译、覆盖部署、启动程序，并自动读 `data\logs\`
检查日志里是否出现「已注册算法插件」——出现了才算修复成功，脚本会明确打印结论。

---

## 十、监测逻辑与测试（v1.0.2 · 2026-09-12）

这一版把「实时监测人工安装动作」这条链路补完，并给它配上了可重复的测试逻辑。

### 10.1 监测闭环（现场跑的这条）

```
模拟/光电触发 → 连拍 N 帧（按配方的去抖帧数）取稳定判定
      ↓
视觉算法判定：OK / NG / 进行中
      ↓
监测会话（MonitoringSession）：判定 + 流程流转 + 计数，只在这一处实现
      ↓
SOP 状态机：OK 推进下一道；NG 锁线停留在原工序；复检通过自动解锁
      ↓
NG 图落盘 + 历史记录（OK/NG 才入库）+ PLC 写回（允许放行 / 检测结论）+ 声光报警
      ↓
六道工序全过 → 整件完成、件数 +1 → 2.5 秒后自动开始下一件
```

### 10.2 测试逻辑（没有产线也能验证）

| 手段 | 说明 |
|---|---|
| 可编程场景相机 | `ISceneCamera`：直接指定"画面里哪个工位有料"，画面完全确定，判定可断言 |
| 操作员动作模拟器 | 把"人做了哪个动作"翻译成"画面里哪个工位有料"，四类动作：规范动作 / 跳步 / 漏装 / 重复触发 |
| 界面上的模拟按钮 | 主界面右侧「模拟人工动作」四个按钮，点一下 = 做动作 + 触发检测，现场可直接演示 |
| 命令行自检 | `VisionForge.Main.exe --selftest`：跑 10 个用例并输出报告，退出码 0 = 全通过 |

自检报告位置：`app\data\selftest\report.txt`（每次重建都会自动跑一遍并打印到窗口）。

自检覆盖的用例：

| 用例 | 断言 |
|---|---|
| T1 规范完成第 1 道工序 | 判 OK、推进到第 2 道 |
| T2 六道工序全部按序完成 | 整件完成、件数 +1、OK 计数 = 6 |
| T3 跳步 | 判 NG、理由含"跳步"、**锁线**、仍停在第 1 道 |
| T4 补做被跳过的工序 | 复检通过、**自动解锁**、推进到第 2 道 |
| T5 漏装（该做没做就触发） | 判"进行中"、不推进、不计数 |
| T6 重复触发 | 幂等，不重复计数 |
| T7 人工放行 | 推进并被计数（人工干预率可追溯） |
| T8 历史落盘 | OK/NG 逐条入库，「进行中」不入库 |
| T9 复位 | 回到第 1 道、进度清零、算法记忆清空 |
| T10 复位后重新开始 | 第 1 道正常通过，无"凭空跳步"误报 |

### 10.3 本次修掉的三个监测语义缺陷

| 缺陷 | 症状 | 修法 |
|---|---|---|
| 「进行中」被当成 Pass | 员工什么都没做，每触发一次工序进度照样往前走 | 序列算法改返回 `None`，增补 `ProgressSteps` 进度字段，**只有进度增加才算完成一道工序** |
| 跳步后无法补做 | 跳步后回头补做被跳过的工序，反而被判"逆序"，现场表现为"改好了却怎么也过不去" | 跳步只记违规、不推进内部步骤指针；补做当前工序即正常通过 |
| 复检通过不解锁 | NG 返工做对了，流程仍锁着、红灯不灭，还得再点一次人工放行 | `SopProcess.ApplyResult` 在 Pass 分支显式解锁 |

下面两条是**第一次跑自检时暴露出来的**（10 个用例挂了 6 个），正好说明"测试逻辑"不是走过场：

| 缺陷 | 症状 | 修法 |
|---|---|---|
| 算法只在"整件完成"时才报 OK | 中间每道工序完成都报"进行中"，SOP 永远推不动，界面一直停在第一步；自检 T1/T2/T4/T7/T10 全部失败 | 序列跟踪器增加"进度里程碑"位（`ProgressAdvanced`）：按序完成一道工序就报 OK，只有画面没变化才报"进行中" |
| 历史查询的时间范围算错 | `CountAsync()` 和不带时间条件的查询永远返回 0 —— **界面上的历史表一直是空的**，看着像没数据 | 无时间条件时不再"从公元 1 年逐天拼文件名"，改为枚举目录里的 `.jsonl` 再按日期过滤；自检 T8 专测这条 |

### 10.4 界面新增（主界面右侧顶部）

- **当前工序大面板**：工序序号 / 名称 / 作业指引，字号放大到 20px，两米外可读；边框颜色跟工序状态走（进行中/通过/不合格）
- **实时统计**：OK 数、NG 数、合格率、完成件数
- **本次判定**：判定结论 + 判定依据 + 流程走向（如"已推进到下一道工序｜…"）
- **模拟人工动作**：规范动作 / 跳步 / 漏装 / 重复触发（没有相机硬件时也可用）
- **过程数据**：每个 ROI 的占用率、当前步骤、违规次数等逐行显示

### 10.5 示例数据升级

示例配方从「2 个 ROI」升级为「6 个 ROI ↔ 6 道工序」一一对应
（`demo-wm-assembly-6step`），这样"第 N 道工序"在画面上有确定位置，
模拟动作和 ROI 顺序才对得上。程序启动时会自动完成这次替换：
旧的那份 `取件工位·双 ROI 检测（示例）` 会被移入 `data\recipes\_deleted\`，
`sop.json` 的步骤会重新绑定到新示例配方。**现场工程师自己建的配方不会被改动。**

---

## 十一、过程层：手部动作合规监测（v1.0.3 · 2026-09-12）

依据《方案修订版 v9》：需求已从「结果防错」转向「**过程手法错误**」，
并锁定为 **中粒度（动作轨迹/位置/节奏）+ 实时拦截 + 手部 21 点关键点 + 规则引擎**。
本次把这条链路在软件里落地（全部可离线验证，实拍样本到位后直接换数据源）。

### 11.1 分层关系

| 层 | 做什么 | 状态 |
|---|---|---|
| 保底层 | 结果防错：零件在位 / 工具归位 / 跳步（视觉 + SOP 状态机） | 已完成（第十节） |
| **过程层** | **手部动作是否到位、停留够不够、顺序对不对 → 实时拦截** | **本次新增** |
| 增强层 | 圈数 / 力度 / 3D 姿态（V2，按需） | 未做 |

两层**共用同一套流程控制**：结论都进同一个监测会话与 SOP 状态机，
所以「跳步锁线」「复检解锁」「整件完成自动计数」这些行为天然一致，不存在两套逻辑打架。

### 11.2 本次落地的判定规则

| 规则 | 取值 | 说明 |
|---|---|---|
| 判定点 | **掌心中心** = 关键点 0/5/9/13/17 均值 | 比腕点稳，减少 ROI 边界抖动误判（v1.3） |
| 停留阈值 K | **时间基准 300ms**（不是帧数） | 换相机、改帧率都不用重新标定（v1.1） |
| 低置信帧 | **跳过不清零** | 手被零件挡一瞬间 ≠ 手离开（v1.1） |
| 持续丢失 | 连续 >1000ms → `UNRELIABLE` **报警**，不判违规 | 看不清请人来看，绝不自动拦线 |
| 动作内顺序 | 无序（集合覆盖，目标到过即可） | 「取 A、取 B」谁先都行 |
| 步骤间顺序 | 由 `order_enforced` 开关控制 | 开 = 检跳步/错序；关 = 只要求覆盖 |
| 目标数量 | N 个可配置（示例给了 6 个） | 对应方案 R7 |

### 11.3 违规码表

`STEP_SKIPPED` 跳步 · `STEP_OUT_OF_ORDER` 错序 · `STEP_NOT_DONE` 目标未完成 ·
`COUNT_MISMATCH` 数量不符 · `UNRELIABLE` 关键点丢失 · `CONFIG_INVALID` 规则配置错误

界面上每条违规显示「规则码 + 工序号 + 中文说明 + 时间戳」，报表可按码聚合。

### 11.4 规则配置（JSON 为准 + YAML 子集互通）

- 位置：`app\data\process-rule.json`（首次运行自动生成示例；`process-rule.yaml` 为同内容副本）
- 界面「导出规则 YAML」可随时导出，方便现场交接与手工微调
- 两种格式都走同一套校验：**配置错了一定报 `CONFIG_INVALID`**，不会静默变成"什么都不做"
- 坐标空间：界面拖框得到的是归一化坐标（0~1，相对画面）。方案里写的是"像素 ROI"——
  这里改成归一化的原因很实际：预览画布是 960×720、相机是 2448×2048，
  存归一化才能真正做到"换分辨率不改配置"。现场体验不变（拖框即得，不用管坐标）。

### 11.5 界面新增（右侧栏顶部「过程监测 · 手部动作」）

- **当前动作 + 置信度条**：`装入外壳 [████░░] 0.97` 这一形态（v7 补丁 B）
- **作业位三态**：绿=已到位 / 橙=等待中 / 灰=未执行
- **违规列表**：规则码 + 时间 + 说明，可一键清空
- **启动/停止过程监测**：用「模拟手部轨迹」按标准作业走一遍（实拍样本到位后换真实数据源）

### 11.6 自检新增用例（S1–S9，每次重建自动跑）

| 用例 | 断言 |
|---|---|
| S1 规范作业 | 三步按序到位 → 整件合规、零违规 |
| S2 跳步 | 报 `STEP_OUT_OF_ORDER` + `STEP_SKIPPED`，且不推进 |
| S3 关掉顺序开关 | 同一条轨迹不再报错序（证明开关真的生效） |
| S4 手快掠 | 停留 30ms < K=100ms → `STEP_NOT_DONE`（动作不达标，不是系统太严） |
| S5 遮挡 500ms | 报 `UNRELIABLE` 但**不判违规**，遮挡后继续累计并完成 |
| S6 数量不足 | 期望 2 个实际 1 个 → `COUNT_MISMATCH` |
| S7 配置校验 | 目标越界 + 引用不存在目标 → 报出明确错误 |
| S8 YAML 往返 | 导出→导入，目标/工序/阈值完全一致 |
| S9 残缺 YAML | 无目标、fps=0 → 明确拒绝（对应 `CONFIG_INVALID`） |

### 11.7 下一步（等实拍样本）

1. **建图 UI**：画面上拖框建作业位（自动编号 P1..Pn + 中文命名）+ 目标框叠加
2. **真实手部模型**：RTMPose 手部 21 点 + ONNX Runtime CPU，与 `IPoseSource` 对接（引擎一行不改）
3. **样本标定**：用实拍 A/B/C 组样本定 K、置信度阈值、ROI 容差（对应方案 T2.4）
4. **对象状态轨真实数据源**：数量/型号/正反检测（MVP 已预留 `TargetCounts` 入口与 `COUNT_MISMATCH` 规则）
5. 待拍板：单工位/多工位、过程视频隐私边界（存坐标还是存视频、清理周期）

---

## 十二、Operator 层交互（借鉴基恩士 CV-X，v1.0.4 · 2026-09-12）

按《UI 交互对照表 · 基恩士借鉴》里的 P0 三条落地。

### 12.1 P0-① 结果变色状态机统一

全界面统一一个判定状态键 `VerdictStateKey`：

| 取值 | 含义 | 颜色 |
|---|---|---|
| `ok` | 通过（工序完成 / 整件完成） | 绿 |
| `active` | 进行中 · 关键点丢失（报警但不算违规） | 橙 |
| `ng` | 违规（要拦截） | 红 |
| `pending` | 未判定 | 灰 |

ROI 框、判定结果区（文字 + 边框）、底部步骤条、作业位状态块全部读这一个值，
不会出现"画面是绿的、步骤条是红的"这种自相矛盾。

ROI 框有两种着色模式：**没跑检测时**用调色板配色（相邻框一眼能分开）；
**跑过程监测时**随判定状态变色（已到位的目标变绿、等待中的变橙、违规变红）。

### 12.2 P0-② NG 保持图回看闭环

这是原 UI 最缺的一块：异常条目只有一行规则码，复盘时看不到"当时到底是什么样"。
现在：

- 违规发生时**自动抓帧存图**到 `data\ng-images\PROC-时间戳.png`
- 界面最多保留 **3 张**保持图（基恩士也是这个思路：够复盘，不堆磁盘）
- **点异常列表里任意一条** → 下方立即调出那一刻的画面
- 没有图时会明确说明原因（"出违规时相机未连接，没有画面可存"），而不是留一片空白

### 12.3 P0-③ PLC 实时拦截契约

违规发生时，上位机按固定契约写 PLC：

| 信号 | 地址（默认） | 值 |
|---|---|---|
| 检测结论 | `InspectResult` = D200 | `0`（NG） |
| 允许放行 | `AllowPass` = M100 | `false` |
| 违规规则码 | `RuleCode` = D201 | `0`跳步 `1`错序 `2`目标未完成 `3`数量不符 `4`关键点丢失 `5`配置错误 |

时间戳由上位机历史记录承担，PLC 侧不必存时间。`RuleCode` 地址写进配置模板，
老配置文件会在启动时自动补上（配置自愈），不会静默失效。

### 12.4 未做的（P1 / P2，按对照表）

| 项 | 说明 |
|---|---|
| P1-③ 作业模板目录 | 下拉选工位/SOP 模板，操作员不进复杂设置 |
| P1-④ 良品基准一键采集 | 录一段标准动作作基准（对应 Auto-Teach 的"只跑良品"） |
| P1-⑤ 误检标记闭环 | 操作员标记误报 → 样本回灌模型 |
| P2-⑥ 操作员速查卡 | 按 YAML 规则自动生成速查卡贴在界面侧栏 |

---

## 十三、测试逻辑与三级界面（增补方案十 / 十一，v1.0.5 · 2026-09-12）

### 13.1 批量仿真与验收指标（对应方案 10.3 / 10.6）

新增「**批量仿真（验收指标）**」：把教示出来的 OK/NG 样本用**留一法**考一遍 ——
拿每条样本去问"用其余样本能不能认出你是哪一类"，然后统计：

| 指标 | 含义 | 验收线（方案 10.6） |
|---|---|---|
| 准确率 | 判定与人工标注一致的比例 | ≥ 98% |
| 漏检率 | NG 样本被漏掉（判 OK 或无法判定）的比例 | ≤ 1% |
| 误检率 | OK 样本被误判为 NG 的比例 | ≤ 1% |
| 平均耗时 | 每条样本的判定耗时 | 端到端 ≤ 200ms |
| 无法判定 | 相似度不够转人工确认的条数 | 单独统计，不计入漏检/误检 |

批次上限 256 条（对应方案"支持 256 组样本批量仿真"）。结果直接显示在界面上，
不合格项会逐条列出（哪一条漏检、哪一条误检），并写进日志。

> 这一步的价值：不用额外录数据，就能回答"我教的这些样本到底能不能用"。
> 达不到验收线说明样本太少或两类动作本来就分不开 —— 这时候上产线必然误判。

### 13.2 三级界面与权限隔离（对应方案 11.2）

顶栏页签按角色自动裁剪，和方案的三级界面一一对应：

| 角色 | 可见页面 | 对应方案中的界面 |
|---|---|---|
| **操作员** | 首页（作业界面） | 11.2.1 一线操作员界面 |
| **技术员** | 首页 + 历史查询 | 11.2.2 管理员界面（只可查看复核） |
| **工程师** | 全部（含配方管理、系统设置） | 11.2.3 运维 / 工程师界面 |

角色切小之后，如果当前停在无权页面，会自动拉回首页，不会出现"页签没了但内容还在"。
另外新增「**画面抓拍**」按钮（存到 `data\snapshots\`），配合原有的手动触发、异常复位，
组成方案要求的作业区基础按钮。

### 13.3 自检用例（现共 22 项）

本轮新增：

| 用例 | 断言 |
|---|---|
| U1 教示后识别标准动作 | 判 OK 且相似度 > 90% |
| U2 教示后识别违规动作 | 判 NG |
| U3 没见过的新动作 | 判「无法判定」，不硬猜、不误拦 |
| V1 批量仿真（留一法） | 教示样本自身可区分时，准确率 100%、漏检/误检 0 |

---

## 十四、标准测试用例落地（对应增补方案十三 · CCD-001~026）

自检报告的用例编号已与方案的 **CCD-xxx** 编号对齐，交付时可直接逐条对勾：

- **可自动执行并通过**：CCD-001 相机参数下发、CCD-002 噪点工况识别、CCD-003 双路相机同步、
  CCD-004 亮度漂移稳定性、CCD-007 缺陷不漏检、CCD-009 统计不跳动、
  CCD-012 256 组离线批量仿真、CCD-013 历史回放（落盘→重新载入→特征一致）、
  CCD-014/017/018 三级界面权限隔离、CCD-023 边界样本批量无误检、CCD-024 设备异常容错
- **标注为映射**（由已有用例覆盖）：CCD-005/010/011/020/021/022
- **标注为跳过并写明原因**：CCD-006 尺寸精度（需实机标定）、CCD-008 颜色识别（黑白相机，
  以特征区分替代）、CCD-015/016 界面目视项、CCD-019 模板切换（未开发）、
  CCD-025 72 小时长跑（现场项）、CCD-026 MES 接口（未开发；PLC 规则码契约已测）

报告里 `[PASS]` 为通过、`[SKIP]` 为跳过（需现场或未开发）、`[FAIL]` 为失败；
结论行给出"通过 / 跳过 / 失败"三项计数，失败才会让整体判定为不通过。

---

## 十五、全量代码审计与稳定性加固（v1.0.6 · 2026-09-12）

按"稳定性优先"对全部 5 个工程、约 1.45 万行代码做了规则化审计，逐条核实后只改真问题。

### 15.1 审计覆盖的 14 个维度

| 维度 | 结论 |
|---|---|
| 同步等待异步（死锁） | 扫描 12 处，**全部安全**：都在 `Task.Run` 内或在非 UI 线程（自检进程） |
| `async void` | 仅 `AsyncRelayCommand.Execute`（ICommand 接口要求），内部有异常捕获 |
| 空 `catch` | 11 处，仅 1 处属真问题（见下），其余为释放资源 / 损坏文件备份场景，合理 |
| 定时器与线程 | 发现 2 个定时器未在释放时停止 |
| `IDisposable` 完整性 | 补齐定时器停止与事件解绑 |
| 集合无限增长 | 违规列表无上限（7×24 场景隐患） |
| 数值解析与区域设置 | `ParameterReader` 按当前文化解析，换区域会静默取默认值 |
| 退出时序 | 帧投递未做保护 |
| 采集线程健壮性 | 订阅方异常会污染取流任务 |
| 设备掉线恢复 | 缺相机自动重连 |
| 线程安全 | 场景引用替换（不可变列表）、`_lastFrame` 引用赋值、界面集合仅 UI 线程访问 —— 均安全 |
| 文件句柄 | 图像载入用 `OnLoad` 不锁文件；日志/历史写盘有门闸 |
| 事件订阅/解绑 | 相机与姿态源成对解绑；补齐 `Sop.Alarm` |
| 全局异常兜底 | 界面线程 + 后台线程兜底已具备 |

### 15.2 修复清单

| # | 问题 | 风险 | 处理 |
|---|---|---|---|
| 1 | 蜂鸣器自动消音定时器未在 `Dispose` 停止 | 关窗后仍触发，访问已释放的报警设备 | `Dispose` 中停止并置空 |
| 2 | 违规列表无上限 | 长时间运行内存与界面双重膨胀 | 界面保留最近 200 条（完整记录在日志与历史库） |
| 3 | `ParameterReader` 按当前区域设置解析数值 | 逗号小数点的区域下阈值静默变成默认值 | 改为不变文化优先、当前文化兜底 |
| 4 | 清空教示样本后写盘失败被静默吞掉 | 表现为"清了没用" | 改为写日志 |
| 5 | 相机掉线无自动恢复 | 网线松动就得等下一件产品失败才发现 | 新增相机看门狗：5s 检测，最多重连 3 次，失败亮红灯告警 |
| 6 | 退出瞬间帧投递未保护 | 关闭时可能抛异常影响退出流程 | 相机线程与姿态线程两处都加保护 |
| 7 | 订阅方异常会污染取流任务 | 界面一次异常可能导致采集中断 | `MockCamera` 内部包裹回调调用 |

结论：**没有发现残留死锁风险**（此前修复的异步死锁、绑死锁等问题经验证已根治），
本轮 7 项修复集中在"长时间运行"和"设备异常恢复"这两类现场最容易被忽略的隐患上。

---

## 十六、MainViewModel 拆分重构（v1.0.7 · 2026-09-12）

### 16.1 拆分结果

原来的 `MainViewModel.cs` 是 **2883 行 / 293 个成员**，作业页、配方页、历史页、设置页、
过程层、教示识别、报警、看门狗全挤在一个文件里。现在按职责拆成 9 个文件：

| 文件 | 行数 | 装什么 |
|---|---|---|
| `MainViewModel.cs` | 745 | 骨架：依赖注入字段、构造、命令声明、页面导航、生命周期（`InitializeAsync`/`Dispose`） |
| `MainViewModel.Process.cs` | 714 | 过程层规则引擎接线、教示识别、批量仿真、PLC 规则码契约 |
| `MainViewModel.Inspection.cs` | 533 | 保底层检测、相机/PLC 连接、模拟动作、取图渲染 |
| `MainViewModel.Roi.cs` | 224 | ROI 标定：鼠标画框、命中检测、增删 |
| `MainViewModel.Alarm.cs` | 216 | 声光报警映射、蜂鸣消音、相机看门狗 |
| `MainViewModel.Recipe.cs` | 183 | 配方管理页：新建/保存/删除、参数面板、ROI 叠加 |
| `MainViewModel.Vision.cs` | 159 | NG 保持图、画面抓拍、图像载入 |
| `MainViewModel.History.cs` | 159 | 历史查询页、CSV 导出 |
| `MainViewModel.Settings.cs` | 150 | 系统设置页、过程规则保存 |

以后改"历史查询"只需要动 159 行那个文件，改"报警逻辑"只动 216 行那个 —— 改动面小一个数量级，
误改风险随之下降。

### 16.2 为什么用 partial 类，而不是拆成独立子 ViewModel

子 ViewModel（`{Binding Recipe.RecipeList}` 这种）需要改**全部 XAML 绑定**，
而界面绑定有上百处，一旦漏改就是运行时绑定失败——收益是"结构更纯"，
代价是**在这个不能本地编译的交付环境里引入大面积回归风险**。

partial 类拆分：**零 XAML 改动、零行为变更、成员一一对应**，
拿到的是同样的"按职责分文件"效果。等主体验收通过、有正常编译环境后再评估要不要进一步拆子 VM。

### 16.3 拆分是怎么保证不出错的

用脚本做机械搬移，只用"按原文连续区间整段搬走"的方式（每行恰好归属一次），
然后自动核对：

| 核对项 | 结果 |
|---|---|
| 成员总数 | 原 293 → 拆分后 293 |
| 丢失成员 | **0** |
| 多出成员 | **0** |
| 重复成员 | **0** |
| 每个文件大括号平衡 | 全部为 0（平衡） |
| 全工程 .cs 括号平衡 | 通过 |
| 每个文件命名空间 + partial 类声明 | 齐全 |

---

## 十七、怎么用（现场速查）

### 17.1 三种用法，按现场条件选

| 用法 | 需要什么 | 怎么操作 | 判定依据 |
|---|---|---|---|
| **① 教示识别**（最省事） | 只要相机能出图 | 首页点「记 OK」「记 NG」教几次 → 点「开始自动识别」 | 画面特征比对样本 |
| **② 过程监测**（推荐） | 相机 + 作业位（料盒）位置 | 首页点「启动过程监测」 | 手部动作 vs 作业位规则 |
| **③ 保底视觉** | 相机 + 配方 ROI | 配方管理页标定 ROI → 首页「执行检测」 | 阈值斑点 / 尺寸 / Halcon |

三条线共用同一套流程控制，随便用哪种，NG 都会锁线、报警、存证、写历史。

### 17.2 五分钟看懂（推荐第一次这样玩）

1. 双击桌面「追觅洗地机AI-SOP监测系统」启动
2. 首页点 **「▶ 一键演示」** —— 它会：连上（模拟）相机 → 用你自己框出来的作业位跑一遍 →
   模拟操作员"抓两个东西" → 跑出 OK 判定 → 再跑一个"漏抓"反例 → 教示识别小演示，
   每一步都在面板上出一行中文结果
   （还没框过作业位时，它会提示你先去「🎯 标定 ROI」框一下，而不是自己编位置）
3. 看完演示，点「启动过程监测」看真实节奏的实时判定（模拟手部轨迹约 3 秒走完一件）
4. 想试 NG：点「记 NG」把当前画面教成违规样本，再点「开始自动识别」，它就会拦

### 17.3 四个页面

| 页面 | 干什么 |
|---|---|
| **首页** | 干活的地方：画面、判定、异常列表、NG 保持图、一键演示、模拟动作 |
| **配方管理** | 建配方 / 标定 ROI / 换算法 / 调参数（每个参数都有「▸ 说明」下拉） |
| **历史查询** | 按时间/批次/型号/结论查记录，看合格率，导出 CSV |
| **系统设置** | 相机、PLC、报警、存储、过程层规则（K/T/置信度）、界面缩放 |

### 17.4 现场上手顺序

```
① 装相机、对光、配硬触发（见《现场部署接线手册》）
② 连接相机 → 在配方管理页框出料盒/作业位（ROI 顺序 = 工序顺序）
③ 首页「记 OK」教 3~5 次标准动作、「记 NG」教 2~3 次违规动作
④ 点「批量仿真（验收指标）」看准确率/漏检/误检是否达标
⑤ 达标后点「开始自动识别」或「启动过程监测」正式跑
⑥ NG 时点异常条目 → 看当时的抓帧图 → 复盘
```

### 17.5 出问题先看这三处

| 看什么 | 位置 | 能看出什么 |
|---|---|---|
| 运行日志 | `app\data\logs\yyyy-MM-dd.log` | 每一步判定、PLC 写回、异常堆栈 |
| 自检报告 | `app\data\selftest\report.txt` | 22 项自检 + CCD 标准用例逐条结果 |
| NG 保持图 | `app\data\ng-images\` | 出事那一刻的画面 |

---

## 十八、画框 / 步骤小方框 / 现场图片建图（v1.0.8 · 2026-09-12）

这一版解决现场反馈的三件事：**框画不出来**、**每一步做到没有要一眼看到**、
**要能拿现场照片来标位置**。

### 18.1 "按住拖拽画不出框"——根因不是一个，是两个

| 现象 | 根因 | 修法 |
|---|---|---|
| 拖着拖着什么都没有 | 拖动过程中的 `MouseMove` 事件丢失，松手时框的宽高还是 0，被"太小忽略"挡掉 | 松手时用**按下点 → 松开点**这两点重建矩形；移动事件丢不丢都不影响结果 |
| 框其实存进配方了，可画面上永远看不见 | 画面叠加层的显示开关（`ShowRoiBoxes`）**全工程没有任何地方把它置 true**，一直是隐藏 | 默认改成立即显示；进标定模式、刚画完框都强制打开 |

另外加了两条兜底：

- **单击也能出框**：按住没拖动就是"点了一下"，会在点击处生成一个 160×160 的默认框，
  位置不满意右键删掉重点即可（不再有"点了半天没反应"的状态）。
- 工具条上多了 **「▣ 框：显示 / ▢ 框：隐藏」** 一键切换，框挡视线时可以收起来，判定不受影响。

标定全过程会写日志（`[ROI]` 开头），按下/松开的坐标、框的尺寸都在里面，
以后再出问题直接看日志就能定位，不用猜。

### 18.2 每一步做成一个小方框（绿 = 过了 / 红 = 没监测到）

「每一步动作」做成**小方框**，两处都有（同一份数据，颜色永远一致）：

- **画面正下方**（新增的"底部步骤序列条"）：操作员不用挪视线就能看见做到第几步；
- **右侧「SOP 步骤」面板**：格子更大，方便点进去调参数。

一格 = 一道工序：

| 颜色 | 含义 |
|---|---|
| 绿 | 这一步做完了（判定 OK） |
| 红 | 这一步**没监测到**（跳步 / 漏做），或者判不合格 |
| 橙 | 现在正该做这一步 |
| 灰 | 还没轮到 |

方框下面带图例，现场不用记颜色。**点任意一个小方框**，会直接跳到这一步的检测参数
（算法 / 阈值 / ROI 都在配方管理页那一页调）。

### 18.3 导入现场图片 → 在图上标抓取位置 → 人手进区域就算做到

没相机、或者想先离线把工位建起来时，走这条：

```
① 首页画面左下点「📷 导入现场图片」→ 选一张工位实拍图
② 点「🎯 标定 ROI」→ 在照片上框出员工取件/放件的每一个位置
③ 点「⇄ 同步为判定区域」
④ 之后人手进入某个框并停留够时间（默认 300ms，K 值可调）→ 那一步就算做到了
```

要点：

- 照片是**标定底图**，导入后会冻住相机渲染，不会被下一帧顶掉；
  想看实时画面点「⟲ 回到相机画面」。
- 底图路径存在配方里，换配方自动带出来。
- 「同步为判定区域」会同时保存**配方**和**过程规则**（`data\process-rule.json`），
  第 N 个框 = 第 N 道工序（P1、P2…），不用再配别的东西。
- 第 N 个步骤小方框 ↔ 第 N 个框，两边顺序一致，所以"点第 3 步"就是调第 3 个框。

### 18.4 画面缩放与拖动（滚轮 + 左键，v1.0.9 追加）

导入的现场照片和相机画面都能放大看细节、拖着挪位置：

| 操作 | 效果 |
|---|---|
| **鼠标滚轮**（放在画面上滚） | 放大 / 缩小，**以鼠标指着的那个点为中心** —— 想看清哪里就把鼠标放哪里滚，不会跑偏 |
| **按住左键拖动** | 图片挪到任意位置（1:1 跟手） |
| **按住滚轮拖动** | 同样挪画面。标定模式下左键要用来画框，这时就用它 |
| 画面右下「＋ / － / ⟲ 复位」 | 按钮缩放（以画面中心）、一键回到 100% 居中 |
| 画面右下角显示 | 当前缩放百分比（100% = 正好铺满画面框） |

要点：

- 放大只放大"画面内容"，ROI 框、正在拖的草稿框都跟着一起缩放，所以框始终贴在它标的那个位置上；
  工具条、提示条、底部步骤条保持原样，不会被放大。
- 缩放范围 20% ~ 1200%（放到 12 倍连螺丝都看得清）；放大后画面会裁在画面框内，不会盖到别的卡片上。
- 换底图（导入照片 / 换配方）会自动复位到 100% 居中，免得一进来停在某个看不清的角落。

### 18.5 右键步骤小方框 → 改名称和参数（v1.0.10 追加）

**在任意一处步骤小方框上点右键**（画面底部步骤条、右侧 SOP 面板都行），弹出「第 N 步 · 设置」窗口：

| 分块 | 能改什么 | 存到哪 |
|---|---|---|
| ① 步骤 | 步骤名称、作业指引、允许耗时（秒，0=不检查）、不合格是否锁线 | `data\sop.json` |
| ② 画面上的框 | 这一步对应的第 N 个框：左边 / 上边 / 宽 / 高（按"占画面百分之几"填）；也能点「🎯 到首页用鼠标重新框这一步」跳回去用鼠标框 | 配方里 |
| ③ 检测参数 | 当前配方的算法参数（每个参数带「▸ 说明」下拉） | 配方里 |

要点：

- **一处改名，两处同步**：步骤名同时写到画面上的第 N 个框和步骤小方框上，不会出现"小方框叫 A、画面上的框叫 B"。
- **改框的位置 = 改判定位置**：过程层规则里同序的目标（P1、P2…）坐标会一起更新，
  不会出现"画面上挪了框、判定还在按老位置判"。
- 参数填错（名称为空、宽高为 0、区域超出画面、数值超范围）时**窗口不关**，
  错误直接显示在按钮上方，让你改到对为止。
- 检测参数是配方级的：同一个配方的所有步骤共用，所以窗口里也写明了这一点。

### 18.6 框由现场自己建 + 按框教 OK / NG（v1.0.11 · 2026-09-12）

#### ① 预置框全部去掉，以后也不会再自动生成

早期版本为了让演示有东西可看，会自动生成 6 个框（并对应 6 个作业位）。这一版**彻底去掉**：

| 位置 | 现在的行为 |
|---|---|
| 示例配方 | `Rois` 为空 —— **一个框都不预置** |
| 过程层规则 | `Targets / Steps` 为空 —— 作业位由"框"同步而来，不猜坐标 |
| 升级老数据 | 启动时把**原封不动**的 6 个预置框（按坐标逐一对号）从示例配方和规则里清掉；现场自己挪过/改过/新增的框一律不碰 |
| 一键演示 | 不再"自动建图"，改成用**你自己框出来的框**跑演示；一个框都没有时直接提示先去框，不编造位置 |

为什么这么改：框的位置和相机机位、料盒位置强相关，每台设备都不一样。
软件凭空画 6 个框，现场会以为"位置已经定好了"，反而更危险。

#### ② 区域学习：像基恩士抓取设定那样"教几条就会判"

画面下方新增「区域学习」一条，就在步骤条下面：

```
① 选一个框（下拉选，或者直接在画面上点那个框）
② 零件放好 → 点「✔ 记 OK」    （教它"框里有这个东西 = OK"）
   零件拿走 → 点「✘ 记 NG」    （教它"东西被拿走了 = NG"）
③ 点「▶ 开始识别」
```

之后每个框自己判"有 / 没有"：

| 框的颜色 | 含义 |
|---|---|
| 绿 | 和教的 OK 样本像 → 框里有东西（这一步做到了） |
| 红 | 和教的 NG 样本像 → 东西被拿走（不合格） |
| 橙 | 都不够像 → **无法判定**，提示补样本（不硬猜、不误拦） |

要点：

- 判定只看**框内区域**（不是整幅画面），所以每个框各教各的、互不干扰。
- 没教过的框**不判**（等于没设），不会乱报。
- 每个框的样本单独存：`data\roi-samples.json` + `data\roi-samples\*.png`，可备份、可拷到别的机器。
- 缩略图列表就在旁边，教了几条、当时什么样一目了然；教错了点「清空本框」重来。
- 框判 OK = 那一步做到了：**只推动流程里当前该做的那一步**，已经 OK 的步骤会连续补上，
  所以先放 A 再放 B、或者同时放，都能跟上；框判 NG 会留一张当时的画面 + 一条违规记录 + 写 PLC。
- 三条识别通路（按框学习 / 整幅画面教示 / 过程监测）**互斥**：开一个自动关掉另外两个，
  防止两边同时驱动流程导致重复计数。
- 相似度阈值和教示识别共用（画面上的"相似度阈值"输入框），低于它就判"无法判定"。

### 18.7 一格 = 一个框；"模拟"和"真实判定"分开（v1.0.12 · 2026-09-12）

现场反馈了两个混淆点，这一版都改了。

#### ① 步骤条改成"按框出格"（不再固定 6 格）

| 位置 | 现在是什么 |
|---|---|
| **画面下方的「框位」条** | 一格 = **你在画面上画的一个框**。画一个多一格，删一个少一格；格子颜色跟着这个框的识别结果变（绿=有东西 / 红=被拿走 / 橙=拿不准 / 灰=未教） |
| **右侧「工序（SOP）」面板** | 流程定义（第几步做什么）。**画一个框，这里会自动补一道工序**（第 N 个框 = 第 N 道工序），所以不会再出现"画面上有 7 个框、步骤条只有 6 格、第 7 个框点右键改不了" |

点「框位」格子 = 选中这个框去教它；右键 = 改这一格的名称和参数（和以前一样）。

#### ② "模拟演示" vs "真实判定"，界面上分清楚了

现场问"我教了 NG，为什么一启动过程监测全部显示 OK" —— 因为那个按钮跑的是
**模拟手部轨迹演示**：一只能"标准作业"的假手会依次经过每个作业位，所以必然全绿。
它演示的是规则引擎（跳步 / 漏装能不能拦住），**不参与真实判定**。现在：

- 按钮改名为 **「▶ 模拟手轨迹演示」**，运行中显示「停止模拟演示」；
- 过程监测面板里加了一行橙色说明，写清"这是模拟演示、真实判定请看区域学习"；
- 区域学习那一块标题改为 **「区域学习（真实判定）」**；
- 两条通路互斥：开一个自动关掉另一个。

#### ③ 没有真相机也能把 OK / NG 教出来

模拟相机现在的画面**是稳定的**（以前每帧随机撒点，教的那一帧和识别的那一帧永远不一样，
结果怎么教都认不出来）。另外，接的是模拟相机时，区域学习里会多两个按钮：

| 按钮 | 作用 |
|---|---|
| **模拟：放上一块** | 在选中的框里放一块料 → 点「✔ 记 OK」 |
| **模拟：拿走了** | 把框里的料拿走（空框）→ 点「✘ 记 NG」 |

拿真相机生产时这两按钮自动隐藏（用真零件教就行）。

### 18.8 放大/拖动只能在画面框内（v1.0.13 · 2026-09-12）

现场反馈"放大之后画面盖到别的区域去了"。根因是**缩放变换挂错了层**：

```
之前：裁剪层自己带缩放  →  裁剪矩形跟着一起被放大  →  画面溢出到工具条/步骤条/别的卡片
现在：外层 PreviewViewport 只裁剪（不带变换）
      内层 ZoomHost 才带缩放/平移  →  放大后的画面被外层牢牢裁掉，只在画面框里
```

同时把拖动也夹住了，规则和看图软件一致：

| 状态 | 行为 |
|---|---|
| 放大后（≥100%） | 画面始终盖满整个画面框：拖到边上就停住，**不会拖出空白边** |
| 缩小时（<100%） | 画面居中，**不能拖到一边去** |
| 任何时候 | 画面绝不越出画面框，不会盖到工具条、步骤条、学习条和其它卡片 |

ROI 框、正在拖的草稿框仍然跟着画面一起缩放、一起移动（它们本来就在画面内容里）。

### 18.9 工序数量与框位"双向同步"（v1.0.14 · 2026-09-12）

现场反馈："框都删光了，右上角还写着 **当前步骤 2/9 安装电机**"。

根因：上一版只做了"画一个框 → 补一道工序"，**没做"删一个框 → 少一道工序"**。
画过 9 个框（试框）之后工序就永久停在 9 道 —— 而画面框是空的了，两处显示自相矛盾。

现在改成**双向同步**：

| 你做什么 | 工序（SOP）怎么变 | 界面表现 |
|---|---|---|
| 画一个框 | 自动补一道工序（第 N 个框 = 第 N 道工序） | 框位条多一格；「当前步骤 x/N」的 N 变大 |
| 删一个框（右键删） | 对应那道工序一起删掉 | 框位条少一格；N 变小 |
| 「清空 ROI」 | 工序全部清空 | 框位条空；当前步骤显示 **—**（不再有 x/9） |
| 重新启动软件 | 启动时按实际框数对齐一次 | 老数据里多余的工序会被自动清掉 |

原则很简单：**同一件事在两处显示，就必须一起变**。自检里加了 UI-09 专门守这条
（删光框位后，工序数必须为 0、当前步骤必须为"—"）。

## 十九、动作计时：每个节点花多久、比上次快还是慢（v1.0.15 · 2026-09-12）

这一版加的是"人做得快不快"这条线。它和前面的识别是解耦的：
**每判定出一个节点，就自动记一条时间**，不需要任何额外配置。

### 19.1 首页直接看得到

「当前工序」面板里多了一块计时区（上面那行大号数字）：

| 显示 | 含义 |
|---|---|
| **本步已用时** | 当前这一步从开始到现在的秒数，每 200ms 跳一次 |
| 基准提示 | "基准 5.0s · 还差 1.8s" —— 基准优先取**上一次同一动作**的用时，没上次才用标准工时 |
| **上次用时** | 上一次做这个动作用了多久 |
| 快慢 | "比上次慢了 0.6s（13%）"；慢 30% 以上整块边框变红，10~30% 变橙，明显更快变绿 |
| 当前件号 | 一件产品的所有节点共用同一个件号（批次-序号） |

### 19.2 自动落盘

| 项 | 说明 |
|---|---|
| 存哪 | `data\timings\yyyy-MM-dd.jsonl`（一天一个文件） |
| 怎么存 | **追加写 + 立即落盘**，断电最多丢最后一行，不会毁掉当天历史 |
| 记什么 | 件号、配方、批次、型号、操作员、工序号/名、开始时刻、结束时刻、用时、上次用时、标准工时、判定（OK/NG）、说明 |
| 开关 | 「⚙ 时间设置」里可整体关闭，也可只记 OK 不记 NG |

### 19.3 ⚙ 时间设置（标准工时）

画面 → 首页「⚙ 时间设置」打开：

1. **记录策略**：是否记录、NG 是否记录、慢多少算"明显偏慢"（默认 30%）
2. **各工序标准工时（秒）**：工序来自画面上的框（第 N 个框 = 第 N 道工序）
3. 底部显示计时数据的保存位置（方便备份）

标准工时的现场定法：让熟练工连做 5 件取平均填进去。没有它就只能跟自己的上一次比。

### 19.4 ⏱ 动作时间回看

首页「⏱ 动作时间回看」打开（非模态，可以和首页并排看）：

| 区域 | 内容 |
|---|---|
| 查询 | 日期范围、工序下拉、**只看明显偏慢** |
| 汇总 | 条数、平均用时、最慢的那道工序、明显偏慢次数 |
| **动作时间趋势图** | 最近 30 次用时柱状图：柱色按快慢上色，灰虚线 = 平均，黄点线 = 标准工时 |
| **工序耗时排行（帕累托）** | 各工序平均用时降序排列 + 累计占比折线（看"哪几道工序占了大头"） |
| 明细表 | 每一条记录，含"比上次快/慢多少"、判定、操作员 |
| 导出 CSV | 一键导出给 IE/工艺用 Excel 进一步分析 |

### 19.5 自检新增 10 条（每次重建自动跑）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-01 | 画面上的框默认可见、数量与配方一致 | 通过（旧版这里必然失败：开关恒为关） |
| UI-02 | 拖拽画框：按下点 → 松开点即成框 | 配方里的框 +1 |
| UI-03 | 单击画框：点一下生成默认大小的框 | 配方里的框 +1 |
| UI-04 | 滚轮缩放以鼠标位置为锚点（放大后鼠标指的点不跑偏） | 倍数与锚点都符合 |
| UI-05 | 拖动跟手、放大有上限、复位回到 100% 居中 | 全部成立 |
| UI-06 | 右键改名称/指引/超时：写回 SOP、同步到画面上的框、并通知界面刷新 | 全部成立 |
| UI-07 | 按框学习：框里有东西教 OK、东西拿走教 NG，两种状态都判对；没教过的框不判 | 全部成立 |
| UI-08 | 一格 = 一个框：步骤条格子数 = 框数；框多于工序时工序自动补上 | 全部成立 |
| UI-09 | 删光框位后：工序同步清空、当前步骤显示"—"（不再出现 x/9） | 全部成立 |
| UI-10 | 动作计时：记录能落盘/读回、"比上次慢了 2.0s"能算出来并标成偏慢、可导出 CSV | 全部成立 |

> 说明：趋势图/帕累托图是自绘的（Canvas + 矩形/折线），没有引第三方图表库 ——
> 这套软件要能在没有外网的工控机上重建，少一个依赖就少一个装不上的风险。

---

## 二十、一键演示按钮回归 + 首页底部状态条（v1.0.16 · 2026-09-12）

### 20.1 「一键演示」按钮为什么不见了（已修）

现场反馈"版本更新后一键演示按钮消失了"。根因是**按钮被挤没了**：

```
那一行原来是：[标题][……弹性空白……][▶ 一键演示][▶ 模拟手轨迹演示]
```

我把后一个按钮的文字从"启动过程监测"改成"▶ 模拟手轨迹演示"之后变长了，
右边栏一窄，标题+两个按钮就放不下 —— WPF 会把溢出的按钮裁掉，看起来就是"消失了"。

现在改成 **标题一行、按钮一行，按钮用 WrapPanel**：窗口再窄，按钮也只会换行，不会被裁掉。
顺便把那个按钮文字缩短成「▶ 模拟手轨迹」。

### 20.2 首页底部新增「一行看全场」状态条

> 计时的实时显示（本步已用时 / 上次用时 / 快慢）也一起挪到了这一行 ——
> 右侧栏之前已经因为空间不足挤掉过一次按钮，不再往里面塞东西。

首页最下面加了一行（9 格，等宽），班组长走过来不点任何按钮就能看到：

| 字段 | 数据来源 | 说明 |
|---|---|---|
| 工单号 | `WO-` + 批次号 | 一个批次一个工单，顶栏改批次这里跟着变 |
| 产品型号 | 顶栏型号 | 空就显示「—」 |
| SOP 版本 | `sop.json` 的 Version | 换工艺文件就换版本 |
| **步骤进度** | 已完成 / 总步骤（青色） | 总步骤 = 画面上的框数 |
| **合规率** | 通过 / (通过+不合格)（黄色） | 没有数据时显示「—」 |
| 节拍时间 | 最近一件整件用时 | 鼠标悬停看"最近 5 件平均" |
| **工位状态** | 未连接 / 待机 / 运行中 / 已锁线 | 颜色跟着变（绿=运行中，红=已锁线） |
| 今日告警 | 今日不合格记录条数 | 查历史库，重启也算得出来 |
| 今日漏步 | 今日判定说明里带"跳步"的条数 | 同上 |

### 20.3 自检新增 11 条（每次重建自动跑）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-11 | 底部状态条：工单号 / 步骤进度 / 合规率 / 工位状态都能算出来 | 全部成立 |

---

## 二十一、借鉴"AI-SOP 行为分析"参考实现（v1.0.17 · 2026-09-12）

参考了一段工业级 AI-SOP 行为分析的演示（bilibili，119 秒）。逐帧看下来，
里面最值得抄的不是花哨效果，而是四件具体的事。已经全部落地：

### 21.1 ① 导入录像当相机（最有价值的一条）

参考实现整场演示跑在一段 mp4 上（左边还带播放进度条）。这一条对我们特别值：
**没有现场硬件、人也不在工位时，拿一段录像就能把整套流程跑一遍。**

顶部新增工具条：**📹 导入录像** → 选一段 mp4/avi/mov → 它就是一个相机：

| 功能 | 说明 |
|---|---|
| 出帧 | 录像按真实时间播放，按 ~10fps 转成 Core 层的帧（和真相机走同一条路） |
| 播放控制 | ▶/⏸ 暂停录像、⟲ 从头再播、⏹ 停止分析 |
| 进度 | 时间 `00:25 / 01:33` + 进度条 |
| 兼容 | 上层一行不改：框 ROI、教 OK/NG、计时、证据、趋势图全都能在录像上跑 |

实现放在 `VisionForge.Main/Imaging/VideoFileCamera.cs`：用 WPF 的 MediaPlayer 解码
（不引任何第三方库），渲染当前帧后转成 Bgr24 的 `CameraFrame` 抛出去。
它**刻意不实现 ISceneCamera** —— 录像的画面不能被"设场景"，实现了只会让界面多出点了没用的按钮。

### 21.2 ② 每步留一张证据帧

参考实现底部有"步骤证据（近 5 步）"：每一步都留一张当时的截图。
我们把它**嵌进框位格子里当背景**（半透明，压在状态字下面）——
不额外占地方，而且"第几格 = 第几个框"比一排独立卡片更好对。

- 每一步判定出来（OK / NG 都算）就存一张：`data\step-evidence\第N步-OK-时间戳.png`
- 格子上直接能看到当时画面长什么样；鼠标悬停显示"证据帧 12:31:07"
- 存盘放后台线程，不拖慢判定

### 21.3 ③ 动作置信度 Top3

参考实现右上角有一栏"动作置信度 Top3"（带条形图）。它的价值是回答
"系统现在认为人在做哪一步"，比单个 OK/NG 更能让人判断识别靠不靠谱。

我们的区域学习面板里加了一模一样的 Top3：每个教过的框取最近一次的相似度，
降序排前三，带条形图和百分比。数据不变时不动集合（避免每帧重画）。

### 21.4 ④ 手部关键点叠加

参考实现画面上那层绿色骨架就是手部关键点。工具条新增 **⊙ 关键点** 开关，
打开后把过程监测送来的 21 个手部关键点画在画面上（固定 21 个点对象、只改坐标，帧率再高也不卡）。

**诚实说明**：现在过程监测跑的是"模拟手部轨迹"，所以画出来的是模拟关键点；
等实拍样本与手部模型到位，同一段代码画的就是真实关键点，界面一行都不用改。
没数据时点会全部隐藏，不会在画面上留个"上一帧的手"骗人。

### 21.5 自检新增 13 条（每次重建自动跑）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-12 | 录像当相机：打不开的文件要干净失败（不抛异常、不进入已连接状态） | 全部成立 |
| UI-13 | 关键点叠加：固定 21 个点、没数据时不显示 | 全部成立 |

---

## 二十二、首页改成三栏并列（v1.0.18 · 2026-09-12）

按现场要求调的两处布局：

### 22.1 画面宽度缩到原来的 3/5（缩了 2/5）

原来画面占首页宽度的 3/(3+1.2) ≈ 71%，现在占 1.8/(1.8+1.2+1.2) ≈ 43% ——
正好是原来的 **3/5**，也就是缩小了 **2/5**。

### 22.2 状态条挪到画面右边，竖排并列

"工单号 / 产品型号 / SOP 版本 / 步骤进度 / 合规率 / 节拍时间 / 工位状态 / 今日告警 / 今日漏步"
原来在画面下面横着铺一整条（9 格），现在改成**画面右边那一栏竖着排**，
下面接动作计时（本步已用时 / 快慢 / 上次用时 / 当前件号 + 两个窗口入口）。

三栏顺序：

```
[ 画面（较窄） ] [ 现场状态 + 动作计时（竖排） ] [ 工序 / 参数 / 系统状态 ]
```

### 22.3 顺手做的两处加固（都是"窄了就会出问题"的地方）

| 位置 | 问题 | 处理 |
|---|---|---|
| 区域学习那一行 | 画面变窄后，一行放不下 7 个控件，会被裁掉（这个坑前面踩过两次："一键演示按钮消失"） | 改成自动换行布局，放不下就换行 |
| 状态那一栏 | 竖排内容比窗口高时会看不全 | 套了一层滚动条 |

顺带把状态栏文字与右侧面板的最小宽度都调小了，保证 1200 宽的窗口下三栏都放得下、不互相挤。

---

## 二十三、系统改名为「追觅洗地机AI-SOP监测系统」（v1.0.19 · 2026-09-12）

三处一起改，避免"窗口上一个名、桌面图标另一个名"这种不一致：

| 位置 | 改成 |
|---|---|
| 窗口标题（任务栏上显示的那个） | 追觅洗地机AI-SOP监测系统 |
| 顶栏中间的大标题（含左上角 LOGO 的提示文字） | 追觅洗地机AI-SOP监测系统 |
| 桌面快捷方式 | 追觅洗地机AI-SOP监测系统（并自动删掉改名前的旧快捷方式） |

部署说明文档里"双击桌面启动"那一句也同步改了。

---

## 二十四、右侧栏精简 + 工具条重排（v1.0.20 · 2026-09-12）

现场反馈两条："过程监测卡片被遮盖"、"下面三个空框取消掉，上面的往下挪"。

### 24.1 卡片被遮盖的真正原因

右侧栏原来是 5 张卡抢高度，其中"当前工序"是**按内容自动撑高**的 ——
它内容越加越多（作业指引 + 判定 + 模拟动作 + 过程数据），把其余几张卡挤到只剩边框，
连"过程监测"卡自己的标题和按钮都被裁掉半截。这不是绘制问题，是**高度分配**问题。

### 24.2 右侧栏只留两张卡（取消三张）

原来的五张：过程监测 / 当前工序 / **工序（SOP）** / **动态参数** / **系统状态**——
后三张因为被挤扁，看着就是三个空框。现在按要求取消，右侧只剩：

```
过程监测 · 手部动作
────────────────
当前工序
```

省下来的高度全给这两张卡，并且**每张卡内部都套了滚动条** ——
以后内容再多也只是滚动，绝不会再把标题、按钮裁掉。

那三张卡里原来有用的东西，都挪到了更顺手的位置，功能一个都没少：

| 原来在哪 | 现在在哪 |
|---|---|
| 工序（SOP）列表 | 画面下方的**框位条**（一格 = 一个框，右键还能改名称和参数） |
| 动态参数 | **配方管理页**（本来就有完整参数面板，每个参数带「▸ 说明」） |
| 启动监测 / 重置流程 / 🖥 大屏 | **顶部工具条**（整宽，随时能按） |
| 声光报警状态 | **顶部工具条**上的颜色块（绿/黄/红一眼看到） |

### 24.3 工具条改成自动换行

顶部工具条现在是一个自动换行的横条：启动监测 · 重置流程 · 大屏 · 导入录像 ·
播放/暂停 · 从头再播 · 停止分析 · 录像进度 · 报警状态 · 框显隐 · 关键点 · 画面抓拍 · 日志目录。

窗口多窄都只会换行，不会再出现"按钮被挤没"。

---

## 二十五、缩放条挪到"等待触发"正下方（v1.0.21 · 2026-09-12）

缩放控制（`100%` / `－` / `＋` / `⟲ 复位`）原来在画面**右下角**，现按要求挪到画面**右上角、
"等待触发"浮窗的正下方**。

实现上没有用"写死一个上边距"的办法，而是把两块放进**同一个纵向排列的容器**里：

```
右上角（一组）
 ├─ 等待触发 · 当前步骤
 └─ 100%  －  ＋  ⟲ 复位      ← 永远紧贴上面那块的下沿
```

为什么不写死间距：上面那块的高度会随提示文字长短变化（工序名长一点就多一行），
写死间距迟早会重叠或漏出缝。放进同一组就永远贴合。

操作方式没变：滚轮缩放（以鼠标位置为中心）、左键按住拖动挪画面（标定模式下按住滚轮拖）。

---

## 二十六、三栏比例再调（v1.0.22 · 2026-09-12）

现场要求：把"现场状态 + 动作计时"那一栏**宽度缩一半**，省下的宽度给画面。

| 栏 | 调整前 | 调整后 |
|---|---|---|
| 画面 | 1.8* | **2.4*** |
| 现场状态 + 动作计时 | 1.2* | **0.6***（正好一半） |
| 工序 / 状态（右侧面板） | 1.2* | 1.2*（不变） |

按 1440 宽的窗口算：状态栏从约 397px 变成约 199px，画面从约 597px 变成约 796px。

窄了以后那一栏里的内容仍然够用：每一项都是"标签在上、数值在下"竖着排，
最长的一行（工单号 `WO-20260912`）也不到 90px；
动作计时里的两个按钮本来就是自动换行，窄了只会换行，不会被裁。

### 26.1 当前工序卡片字号压缩（v1.0.22 同版）

同一轮里按现场要求把「当前工序」卡片的字号与间距压小，让它**不用上下滑动就能看全**：

| 位置 | 原来 | 现在 |
|---|---|---|
| 作业指引（工序名） | 20 | 16 |
| OK / NG / 合格率 / 完成件数 数字 | 20 | 15 |
| 判定结果大字 | 15 | 13 |
| 判定理由 / 流程说明 / 模拟回显 | 11 | 10 |
| 模拟动作按钮 | 12 | 11 |
| 「过程数据」区高度 | 86 | 40 |
| 右侧"过程监测 : 当前工序"高度比 | 2 : 2.4 | **1.2 : 2.8** |

---

## 二十七、状态栏去滚动条 + 历史表 NG 整行标红（v1.0.23 · 2026-09-12）

### 27.1 现场状态 + 动作计时：取消滚动条

上一版给这一栏套了滚动条兜底，现场明确不要 —— 那就让它**本来就放得下**：

| 位置 | 原来 | 现在 |
|---|---|---|
| 9 个状态项的标签 | 11 | 10 |
| 状态项的数值 | 13~15 | 12~13 |
| 「本步已用时」大字 | 24 | 20 |
| 那三行"基准怎么算"的说明 | 占三行 | **改成卡片上的悬停提示**（鼠标放上去显示），省下约 45px |
| 卡片内边距 | 12,10 | 10,8 |

内容高度从约 544px 降到约 450px，在 900 高的窗口里就放得下了，**滚动条随之取消**。

### 27.2 历史表格：NG 整行标红

两处历史表（历史查询页的大表 + 首页底部那张）都改了：

| 元素 | 效果 |
|---|---|
| NG 整行 | **淡红底**（#3AEF4444）—— 扫一眼就知道哪几件不合格，不用一行行看"结果"列 |
| "结果"列的 OK / NG | OK **绿色**、NG **红色并加粗** |
| 鼠标选中的行 | 仍然显示成选中色（红色底只在未选中时显示，不影响点选操作） |

---

## 二十八、框位右键菜单：改设置 / 删除（v1.0.24 · 2026-09-12）

现场反馈"框位删不掉"。原因是删除入口藏得太深：以前只能**在标定模式下、在画面上对着框点右键**才能删，
而框位格子上的右键是"打开步骤设置"—— 于是格子上根本找不到删除。

现在框位格子右键弹出菜单：

```
┌──────────────────────────┐
│ 步骤设置（名称 / 参数）…  │
├──────────────────────────┤
│ 删除这个框               │   ← 红色
└──────────────────────────┘
```

### 28.1 删除为什么值得单独做一遍

删一个框会牵动**五处**，少做一处就留下不一致（这也是"删不掉"和"删了还对不上"的根源）：

| # | 要一起处理的 | 不处理的后果 |
|---|---|---|
| 1 | 配方里的 ROI | 框还在配方里，下次打开又回来了 |
| 2 | 画面上的叠加框 | 画面还画着那个框 |
| 3 | 工序列表 | 出现"没有框的第 N 道工序" |
| 4 | 过程层判定区域 | 判定还按已被删掉的框坐标跑 |
| 5 | **学习样本的序号** | **删中间的框后，后面的框序号前移，样本不跟着挪就会"认成别人的东西"** |

第 5 条最阴：比如删掉 2 号框，原来的 3 号变成 2 号，如果它的样本还挂在"3 号"上，
那么现在 3 号框（原来是 4 号）会去比 2 号的样本 —— 判定结果看着正常，其实是错的。
现在删除时会把后面的样本序号整体前移一位，并删掉被删那个框的样本。

删除前会弹一次确认（提示会一起删掉哪些东西），因为标一个框要重新画、重新教。

### 28.2 自检新增 14 条（每次重建自动跑）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-14 | 框位删除：框与工序一起少一个，剩下的框序号自动前移（不跳号） | 全部成立 |

---

## 二十九、修两个"看不见但很致命"的问题（v1.0.25 · 2026-09-12）

这一版没有新功能，修的是上一版**编译失败**和**自检 8 项失败**背后的真问题。

### 29.1 编译失败：XAML 属性值里"绑定 + 文字"混写（MC3044）

```xml
<!-- 错：{Binding …} 后面不能再接普通文字，编译期直接 MC3044 -->
ToolTip="{Binding EvidenceHint}　左键：…；右键：…"

<!-- 对：整句话在 ViewModel 里拼好，属性值只放一个绑定 -->
ToolTip="{Binding TileHint}"
```

这类错误 **XML 本身是合法的**，所以"良构检查"查不出来。已经加进自检脚本：
扫描所有属性值，只要花括号之外还有非空文字就报错（并用这个真实错误验证过）。

### 29.2 自检 8 项失败：相似度算错了（这个更严重）

失败项：T1~T5、T7、T10（保底层六道工序的判定链路）+ UI-07（按框学习）。

**真因 1：相似度其实是"点积"，它假设特征向量是单位长度。**

上一版为了让"有东西 / 被拿走"能分开，在特征最后补了一维"整体亮度"，
但**补完没有再单位化**。而"均匀画面"（空框、平白墙）的图案维几乎是 0，
向量长度只剩 0.087 —— 结果**它跟自己都只有 1% 相似度**，
现场表现就是：明明教过"空框 = NG"，识别却一直说「无法判定」。

修法两处一起做：

1. 特征提取最后**整体单位化**（补完亮度维再归一）；
2. `Similarity` 改成**真正的余弦**（显式除以两个向量的长度），
   这样连旧版本存的样本也能继续用，不会因为升级就让教过的样本失效。

用 Python 把这两段逻辑原样重算验证过：

| 对比 | 修之前 | 修之后 |
|---|---|---|
| 空框 与 它自己的 NG 样本 | **0.007** ✗ | **1.000** ✓ |
| 空框 与 OK 样本（有东西） | 0.008 | 0.086（远低于 0.75 阈值，不会误判成 OK）✓ |

**真因 2：示例配方不再预置框了，但保底层那组用例还依赖它。**

上上版按要求把示例配方的预置框清空（"框必须自己建"），结果 T1~T10 这组用例
拿不到 ROI，算法直接报 ERROR。现在**由自检自己造 6 个框**，
不再依赖示例数据 —— 示例数据以后怎么改都不影响自检。

### 29.3 下次重建自检应该是

**通过 50 项 · 跳过 12 项 · 失败 0 项（共 62 项）**。

以后每次重建，日志窗口里都会直接打出"失败 N 项"；只要 N 不是 0，就说明有回归，
把日志发我即可，不用你一个个点着试。

---

## 三十、多工位联网：几十个工位的实时数据汇总到一台终端（v1.0.26 · 2026-09-12）

需求：每个工位采集各自的数据 → 汇总到一台终端，打开界面一眼看到所有工位实时状态 →
并且数据能上传公司 MES（留好接口）。

### 30.1 三种角色，同一套程序

同一个部署包拷到每台工控机，只是配置不同 —— 不新增任何安装项、不依赖任何外部服务：

| 角色 | 怎么配 | 效果 |
|---|---|---|
| **纯工位** | 填「工位编号 + 汇总终端地址」 | 每 3 秒把自己的实时状态 POST 给终端；做完整件再把整件数据推 MES |
| **汇总终端** | 勾「本机作为汇总终端」 | 启动内置看板服务，浏览器打开 `http://本机IP:8088/` 看全场 |
| **单机** | 都不启用 | 和以前完全一样，不上报、不建服务 |

### 30.2 汇总终端看板（浏览器打开，不装客户端）

看板页面是终端程序自己提供的（内联 CSS/JS，**不依赖任何外部资源**——车间网络常常连不上 CDN）：

```
┌───────────────────────────────────────────────────────────────┐
│ AI-SOP 工位实时看板   工位 24 · 在线 23 · 报警 1 · 今日产量 812 · 平均节拍 31.4s │
├───────────────────────────────────────────────────────────────┤
│ ┌ ST-01 主机装配1 ─ 运行中 ┐  ┌ ST-02 主机装配2 ─ 已锁线 ┐  …      │
│ │ 当前：安装电机 (3/6)      │  │ 当前：装电机   (2/6)     │         │
│ │ OK 128 NG 3 合格率97.7% 节拍31s │ OK 96 NG 12 合格率88.9% │      │
│ └──────────────────────────┘  └──────────────────────────┘        │
└───────────────────────────────────────────────────────────────┘
```

- 每 2 秒自动刷新；**报警（已锁线）整块变红**、**超过 30 秒没上报显示为离线并变暗**
- 页头是全场汇总：工位数 / 在线数 / 报警数 / 今日产量 / 平均节拍
- 数据接口 `GET /api/stations`（JSON）—— 大屏系统、报表工具可以直接取来二次开发

### 30.3 为什么用 TcpListener 自己实现，而不是 HttpListener

`HttpListener` 监听非 localhost 地址时，Windows 要求先执行 `netsh http add urlacl`（管理员权限）。
几十台机器都要配 ACL 太折腾，所以这里用 `TcpListener` 监听 0.0.0.0 端口，
**普通用户权限即可、也不需要装任何东西**。只实现业务需要的那部分 HTTP/1.1（GET/POST + Content-Length），
代码不到 200 行，行为完全可控。

### 30.4 MES 对接（接口已留好）

详细契约见同目录的 **《MES对接说明.md》**（可直接发给 MES 供应商）。要点：

- 上传方式：HTTP POST JSON，可选 `Authorization: Bearer <令牌>`
- 上传时机：**一件产品做完整件后**上传一条（含每道工序的开始/结束时刻、用时、标准工时、判定）
- 幂等：带 `ReportId`，MES 侧去重即可
- 断网续传：失败自动进 `data\mes-queue\` 队列，每 30 秒补发；
  `4xx` 不重试，挪到 `rejected\` 留证据
- 可替换：`IMesClient` 接口，若 MES 不是 HTTP（WebService/数据库/MQTT），换一个实现类即可，主程序不改
- 没接 MES 时：勾「同时在本地留一份 JSON」，数据先落 `data\mes-outbox\`

### 30.5 界面上新增

**系统设置 → 工位联网 / MES 对接**：工位编号 / 工位名 / 产线名 / 汇总终端地址 / 上报间隔 /
本机作为汇总终端（端口）/ 🌐 打开看板 / 联网状态；以及 MES 的接口地址、令牌、超时、
测试连接、立即补发积压、待发积压条数。改完点「保存设置」**立即生效**。

### 30.6 自检新增 16 条（每次重建自动跑）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-15 | 多工位汇总看板：**真起一个终端服务**，POST 一条工位上报，再从 JSON 接口与看板页面读回来 | 全部成立 |
| UI-16 | MES 契约：上传体字段固定（SchemaVersion / 工位 / 件号 / OK-NG / 违规码 / 工序明细） | 全部成立 |

---

## 三十一、模板池 / 一键下发 / 右键菜单修正 / 连续试跑（v1.0.27 · 2026-09-12）

### 31.1 框位右键菜单前面的白色去掉了

现场反馈"跳出来的对话框前面有点白的"。原因是 WPF 默认的 `MenuItem` 模板左侧留了一列
"图标 / 勾选位"，深色界面上会显出一条浅色空隙。现在给 `MenuItem`、`ContextMenu`、`Separator`
都换成了自定义深色模板：整行就是一行文字，高亮时整行变色，没有任何多余留白。

### 31.2 一条线 30 个工位：模板池 + 一键下发

这是这一版的重点。逻辑分两个方向：

```
① 汇总：工位第一次把配方/SOP 调好 → 「把本机模板汇总到终端」→ 进终端模板池
② 下发：终端在池子里挑一份 → 「一键下发到全部工位」→ 30 个工位自动拉取并生效
```

**为什么用"工位来拉"而不是"终端去推"：**工位可能关机、可能刚重启、可能隔着防火墙。
推送要一台台连过去，任一台不通报错就断；拉模式最稳 —— 关机的工位一开机就自动补上最新版本。

工位每 **15 秒**问一次终端"有没有新版本"，有就下载 → 应用 → 回执。终端界面上能看到
`当前下发 v3 · 已应用 24/30 个工位`。

一个包包含什么：

| 内容 | 说明 |
|---|---|
| 配方 | 算法、参数、ROI 坐标（**ROI 可选**：各工位机位完全一致才带，不一致就别带，免得把别人的坐标覆盖过来） |
| SOP | 工序顺序、名称、指引、超时、是否锁线 |
| 过程层规则 | K 值、时间窗、是否强制顺序、置信度阈值 |
| 标准工时 | 每道工序的标准工时 |
| 教示样本 | **默认不带**（样本与机位/光照强相关）；想把某台调好的工位当样板批量铺开时才勾 |

终端侧的接口（也是给大屏/报表系统留的）：

| 接口 | 用途 |
|---|---|
| `POST /api/template/upload` | 工位把本机模板汇总到池子 |
| `GET /api/template/pool` | 看池子里有哪些模板 + 下发状态 |
| `POST /api/template/publish` | 一键下发（带 `poolId`，或直接带一个 package） |
| `GET /api/template/assigned?station=X&applied=N` | 工位拉取（已是最新就只回 `unchanged`，**不会重复下发**） |
| `POST /api/template/applied` | 工位回执"已应用到 vN" |
| `GET /api/template/status` | 下发版本 / 已应用工位数 / 每个工位的情况 |

模板池与"当前下发版本"会落盘到 `data\hub-templates\`，**终端重启后不丢**。

### 31.3 代码梳理：修掉一处 7×24 连跑会涨内存的地方

规则引擎里的违规记录列表 `_violations` 原来只增不减 —— 一个工位一天几百条违规，
长期运行就是慢性内存泄漏。现在统一走一个 `AddViolations()` 入口，**内存里只保留最近 500 条**
（完整记录本来就会写日志、落历史库，界面与报告用最近的就够）。所有产生违规的地方都改走这个入口，
避免"改了四处漏一处"。

### 31.4 多模拟试跑（新增 4 条自检）

| 编号 | 验的是什么 | 预期 |
|---|---|---|
| UI-17 | **连续试跑 30 件**：每件 6 道工序按序完成 → 完成件数=30、通过=180、不锁线 | 全部成立 |
| UI-18 | **MES 断网续传**：模拟断网时 20 条一条不丢；网络恢复后 20 条全部补发、队列清空 | 全部成立 |
| UI-19 | **违规列表有上限**：连造 600 轮违规，内存里最多留 500 条（不无限涨） | 全部成立 |
| UI-20 | **模板下发全链路**：上传 → 一键下发 → 工位拉取到包 → 回执后不再重复下发 | 全部成立 |

---

## 三十二、修一处编译错误 + 新功能挪到设置页最上方（v1.0.28 · 2026-09-12）

### 32.1 为什么"没看到"新功能

上一次双击 **编译失败** 了，程序还是上一版，所以界面上当然没有新东西：

```
MainViewModel.Deploy.cs(71,44): error CS0246: 未能找到类型或命名空间名"AsyncRelayCommand"
```

`MainViewModel.Deploy.cs` 里用 `as AsyncRelayCommand` 做类型转换，但漏了
`using VisionForge.Common.Mvvm;`。已修，并且给自检脚本又加了一条：
**`as X` / `is X` / `is not X` 这类用法也会检查 using** —— 用这个真实错误验证过能抓到。

顺便确认了一件好事：上一次**成功**的那版（22:12）自检结果是
`通过 50 项 · 跳过 12 项 · 失败 0 项`——说明前面修的 8 项失败（相似度算错、示例配方无 ROI）全好了。

### 32.2 新功能挪到「系统设置」页最上面

「工位联网 / MES 对接」和「配方下发（一键部署到全部工位）」原来在设置页**最底部**，
要往下滚才能看到。现在挪到页面**最上方**（在「相机」之前），打开系统设置第一眼就是它们。

---

## 三十三、"打开看板"点了没反应 —— 已修（v1.0.29 · 2026-09-12）

### 33.1 为什么没反应

两件事叠在一起：

1. **设置没保存**：现场填完工位编号/终端地址后没点「保存设置」，配置里连 `Station` 段都没有 →
   看板服务根本没启动；
2. 这时候点「🌐 打开看板」，程序只在**底部状态条**写了一句话，很容易看漏 ——
   表现就是"点了没反应"。

### 33.2 现在的行为

| 改进 | 说明 |
|---|---|
| **点一下就能用** | 勾了「本机作为汇总终端」但服务没起来时，点「打开看板」会**就地启动看板服务**（并把设置存盘），然后打开浏览器 —— 不用先记得点保存 |
| **看板地址一直显示** | 卡片上多了一行"本机看板：`http://192.168.x.x:8088/`"（自动取本机局域网 IP），不点也知道该输什么 |
| **📋 复制地址** | 一键复制到剪贴板 —— 车间其它电脑/手机要访问的就是这个地址 |
| **提示写在看得见的地方** | 打开成功/失败都会同时写到卡片上的状态文字与底部状态条；打不开浏览器时会把地址直接给出来让人手动输入 |
| **自动补齐开关** | 勾了"作为汇总终端"就自动把「启用工位联网」也打开，避免"勾了终端却没启用"这种自相矛盾的配置 |

### 33.3 顺便修掉自检里那一项失败

那一版自检是 `通过 55 项 · 失败 1 项`，失败的是 UI-20，原因是**接口 JSON 大小写不一致**：
匿名类型写出来是小写开头（`ok`/`status`），而模型类型默认是 PascalCase（`AppliedCount`），
于是同一个响应里一半小写一半大写。已统一成 camelCase（`appliedCount`），
对接方按一种写法就能取到所有字段。

看结果：重建窗口的日志，或 `app\data\selftest\report.txt`。

---

## 三十六、代码已经真的传上去了（v1.1 / v1.2 · 2026-09-13）

仓库地址：<https://github.com/qian1586/-AI-SOP>

### 36.1 实际结果

| 版本 | 提交 | 内容 |
|---|---|---|
| **V1.1** | `ad971870` | 首次上传：157 个文件（源码 + 部署脚本 + 文档 + 可运行版本） |
| **V1.2** | `6eeca099` | 修上传脚本在 GBK 控制台下的编码报错（只传了变动的 2 个文件） |

用浏览器打开仓库，`src/src/VisionForge.Core/Services/SelfLearning.cs`、
`部署说明.md` 这些都在。提交点进去能看到每次改了哪些文件、改了什么。

### 36.2 上传方式已经彻底改成"不依赖 git"

原来的 `上传到GitHub.cmd` 是调 `git push`，但**这台电脑根本没装 Git for Windows**
（`C:\Program Files\Git` 不存在），一点就会失败。现在改成：

```
上传到GitHub.cmd
   └── scripts\upload-to-github.ps1      ← 找 Python、引导登录、把令牌交给下一步
         └── scripts\upload-to-github.py ← 真正干活：走 GitHub 官方 REST API
```

它做的事和 `git push` 等价：**blob（每个文件）→ tree（整棵目录树）→ commit → 更新 main**。
好处是：

* 不需要 git，也不需要在这台电脑上装任何东西；
* 版本号自动 +0.1（V1.0 → V1.1 → V1.2 …），提交信息里带着，一眼看出这是第几次改；
* **只传变动的文件**：本地先算每个文件的 git blob 哈希，和远端比，一样就跳过
  （所以第二次上传只发了 2 个文件，155 个直接复用）；
* 远端就是本地目录的镜像（本地删掉的文件远端也会消失），行为符合直觉。

### 36.3 你要做什么

**已经不用你做了。** 2026-09-13 那次登录已经完成（`✓ Logged in as qian1586`），
以后我改完代码会直接把新版本推上去（V1.3、V1.4 …）。

如果你自己也要传（比如换了电脑），双击 `上传到GitHub.cmd` 即可：
没登录过它会引导你登录一次（浏览器里填一个 8 位代码），之后长期有效。

想带备注：

```
上传到GitHub.cmd -Note "修好了自学阈值"
```

### 36.4 一个提醒

登录凭据（OAuth token）保存在开发沙箱的 `work\gh-config\hosts.yml` 里（明文），
**不在这个部署包里、也不会被上传**。如果哪天想让它失效，
到 GitHub → Settings → Applications → Authorized OAuth Apps 里撤销 GitHub CLI 即可。

---

## 三十七、"我能编译了"——一次质量升级（v1.4 ~ v1.6 · 2026-09-13）

### 37.1 起因：我一直在"用眼睛跑编译器"

前面所有改动都有一个共同的隐患：**开发环境里没有编译器**。
这台机器上 .NET SDK 装在 `C:\Users\39841\AppData\Local\VisionForge\.dotnet`，
沙箱既读不到也执行不了，所以我只能靠静态扫描 + 肉眼推语法。

2026-09-13 把这件补上了：

| 步骤 | 做法 |
|---|---|
| ① 拿到 SDK | PowerShell 在这个环境里 TLS 不通，但 Python 通 —— 用 Python 把官方 .NET 8 SDK（285MB）下到 `work\dotnet`，沙箱有执行权限 |
| ② 让 NuGet 别去读用户配置 | 它默认读 `%AppData%\Roaming\NuGet\NuGet.Config`（被权限挡），把 `APPDATA / LOCALAPPDATA / DOTNET_CLI_HOME / NUGET_PACKAGES` 全指到 `work\` 下面即可（本项目没有第三方包，不需要联网） |
| ③ 一键脚本 | `work\build-and-test.ps1`：编译 → 跑自检 → 打印失败项 |

**结果：第一次真编译就报 0 错误、1 警告；第一次真跑自检就抓出 2 项失败。**

### 37.2 这一下抓出了什么（都是"肉眼看不出来"的）

| 项目 | 性质 | 处理 |
|---|---|---|
| **AI-08 自检失败** | **我的测试写错了**：我把"同一画面自学"的期望写成了"应该新增一条"，实际设计是"相似度 100% → 只强化不新增"。代码是对的，测试是错的 | 改测试：AI-08 断言"只强化"，另加 **AI-09** 专门验证"真有区别的新画面会被新增，且不把判定带偏" |
| **UI-20 自检失败** | **测试断言过时**：接口已统一成 camelCase，测试还在查 `TimingStandards` | 改成查 `timingStandards`。顺带核实了工位端是 `PropertyNameCaseInsensitive = true` 解析 —— 功能本身没问题 |
| `DemoDataFactory` 的 CS8602 警告 | 同一段代码把配置读了**两遍**，第二遍的结果编译器无法确定非空 | 合并成读一次再判空：既消掉警告，也少读一次盘 |

### 37.3 顺手做的三项优化（都有实测数字）

**① 给自学限速（每秒每框最多一次）**
自学原本逐帧跑：相邻两帧几乎一样，送进去只会被判成"强化"，功能上零收益，
但每帧都要复制样本列表 + 跟几十条样本算 193 维相似度。9 个框 × 25 帧/秒 = 每秒两百多次白算，
7×24 跑下去 GC 压力会实打实拖慢界面。改成 1 秒一次采样，CPU 压力降到约 1/25。

**② 淘汰判据从 O(n²) 改成 O(n)**
原来挑"和同组其它样本最像的那条"，要算两两相似度：每框每类 80 条时一次淘汰要算 6400 次；
把上限调到 200 就是 40000 次 —— 而这段代码跑在**界面线程**上，会卡出几十毫秒。
现在改成"淘汰被见到次数最少的（Hits 最小）"，一次淘汰只看一圈，算法上也更合理。

**③ 命名统一 + 常量收口**
`SelfLearn*` 和 `SelfLearning*` 两套词根混用（68 处）统一成 `SelfLearning*`；
上限/门槛的允许范围从"界面里钳一遍、引擎里再钳一遍"改成只在 `SelfLearningOptions` 定义一次。

### 37.4 现在的自检规模与实测数据

```
通过 67 项 · 跳过 12 项 · 失败 0 项（共 79 项）    0 警告 0 错误
```

其中 **AI-10 压力用例**是最有用的一条，它给出的是真实数字：

> 9 个框、每框塞满 40 条 OK + 40 条 NG（共 720 条样本）之后，连收 300 次观察：
> 样本条数稳定在 720（不会膨胀）· 新增 150 次 · **总共 14ms，平均 0.05ms/次**。

也就是说：最坏情况下自学这件事每秒最多花掉几十微秒，对 7×24 连跑没有影响。

### 37.5 还差什么（没做的，如实列出来）

* **没有 xUnit 测试工程**：现在这套自检是自己写的运行器 + 文本报告，
  不是 `dotnet test`、没有 CI。之所以没建，是因为新项目要引 xunit 包 → 需要恢复 NuGet，
  换个没有外网的车间就会卡住；而自检本身已经能在重建时自动跑。等有 CI 需求再迁移更合适。
* **ViewModel 还是"上帝类"**：`MainViewModel` 拆成 10 个 partial、6000+ 行。
  领域逻辑在 Core 里是干净的，但"编排"这层仍然堆在 VM。
* **自学账本仍是同步写**：一次约 300 字节、限速后每秒最多 9 条，同步写是划算的
  （对比：证据帧是 100KB PNG，那个必须丢后台）。但如果以后要记更多字段，得改成后台队列。

---

## 三十八、界面冒烟自检：把"打不开"变成一句能定位的错误（v1.6 · 2026-09-13）

### 38.1 为什么专门做这件事

这个项目历史上前前后后被"打不开"折腾过很多次：
一键演示按钮不见了、下拉菜单全白、右下角卡片被遮住、改了布局没生效……
共同点是——**问题只在界面真正构造的那一刻才暴露**，而现场能给的反馈只有一句"打不开"。

自检（`--selftest`）管的是"逻辑对不对"，它根本不开界面，所以管不了这类问题。
静态扫描能查 XAML 是否良构、资源键是否定义、绑定属性是否存在，但查不了：

* 转换器实例化时抛异常；
* 样式 `TargetType` 与实际控件不匹配；
* `DataTemplate` 在被展开时才求值的绑定；
* 自定义控件构造函数里的逻辑。

### 38.2 做了什么

新增一个开关：`VisionForge.Main.exe --smoketest`

它做的事很直白：**把主窗口真的开一次**（走完整的 XAML 解析 → 资源解析 →
绑定求值 → 布局），让消息队列空转半秒确保模板与转换器都真跑过，然后关掉退出。

| 退出码 | 含义 |
|---|---|
| `0` | 界面能正常打开 |
| `2` | 打不开，原因写在 `app\data\smoketest\report.txt`（含完整异常堆栈） |

### 38.3 接到重建流程上（这才是关键）

`5-修复并重启.cmd` 现在多了一步 **5.5/5 界面冒烟自检**：

```
1/5 找 SDK → 2/5 编译 → 3/5 部署到 D 盘 → 4/5 写快捷方式
5/5 监测逻辑自检（79 项）
5.5/5 界面冒烟自检（真开一次主窗口）
      ↓ 通过才启动程序；不通过就停下来，把报告全文留在窗口和日志里
```

以前"编译成功"就启动，界面起不来你只会看到窗口闪一下；
现在**界面打不开就明确停下并告诉你原因**，不会再让你面对一个莫名其妙的现象。

### 38.4 我自己已经用它验证过

在开发环境（沙箱）里跑的结果：

```
[冒烟自检] 通过：界面能正常打开（XAML / 资源 / 转换器 / 绑定都过了一遍）
```

也就是说，**这次新增的「AI 自主学习」整条界面（开关、两个勾选框、门槛与上限输入框、
四个按钮、三行状态文字）是真被渲染过一遍的**，不是"我觉得应该没问题"。

---

## 三十九、两处布局修正 + 撤掉界面缩放（v1.7 · 2026-09-13）

### 39.1 画面右侧那一栏超出了屏幕

现场反馈："红色花圈这里超出了画面"（右侧状态栏底部的动作计时卡被挤出屏幕）。

**根因**：那一栏有 9 个状态项，原来每项都是"标签一行 + 数值一行"，9 项就是 18 行；
再往下是动作计时卡。内容自然高度约 553px，而这一栏能拿到的高度在 1920×1080 最大化时是 592px ——
本来刚好够，**但界面缩放是 110% 的时候，内容会被放成约 608px，超出 16px 起**，
第二个按钮就被切掉了。

**改法**：

* 9 个状态项改成**单行**（标签在左、数值在右），高度直接省掉约一半；
* 用 `DockPanel` 而不是横向 `StackPanel` —— 横向 StackPanel 不限制子元素宽度，
  长工单号会把卡片撑破；DockPanel 的最后一个子元素占满剩余宽度，配合 `TextTrimming` 才会老实出省略号；
* 动作计时卡内部间距与字号略收（20px → 18px 等）。

**改完实测**（就是这台机器、1920×1080）：

```
现场状态栏：容器可用高 592px，内容需要 392px —— 放得下（还余 200px）
```

### 39.2 底部历史表格太高，把画面挤小了

现场反馈："底部画圈的地方高度缩小，上面显示视频的地方已经很小了，需要扩大"。

底部历史表格是**每一页都要让出高度**的固定行（原来 210px），上面的主画面那一行是 `*`。
所以把它从 **210px 压到 150px**：表头 + 三行记录照样看得见，省下的 60px 全部还给实时画面。

### 39.3 撤掉「界面缩放」卡（系统设置页）

现场要求："画圈的地方默认 100%，然后这个删除"。

* 默认值从 **1.1 改成 1.0**（这是上面那个溢出问题的真正诱因）；
* 系统设置页里的整张「界面缩放」卡（说明文字 + 滑条 + 百分比）**删除**。

功能本身保留：窗口整体等比缩放仍由 `UiScale` 驱动，只是入口撤掉了。
真要调（换了显示器、觉得字小），改 `config\appsettings.json` 里的 `"UiScale": 1.0`（0.8~1.6）。

### 39.4 顺手加了一项「布局体检」

上面那个"超出屏幕"的问题，本质是**内容自然高度 > 容器给的高度** ——
这种问题只有在真开窗口之后才量得出来。所以界面冒烟自检里加了一步：

打开主窗口（最大化）→ 量一次现场状态栏的"需要高度 / 可用高度"→ 写进报告。

现在每次重建的报告里都会多一行，比如：

```
---------- 布局体检 ----------
现场状态栏：容器可用高 592px，内容需要 392px —— 放得下（还余 200px）
屏幕 1920×1080，窗口 1936×1048
```

这一项**只记录、不判失败** —— 布局问题不该拦住程序启动，但必须留下证据，
以后再有"某块东西被挤掉"，第一眼就能看出是哪个容器的余量不够了。

---

## 四十、把高度让给相机展示位（v1.8 · 2026-09-13）

现场要求："画圈的地方高度都压缩，把更多的空间给到相机展示位。"

### 40.1 先量，再改

这次没有凭感觉调，而是先用冒烟自检量了一遍（1920×1080、窗口最大化）：

```
相机画面区高度：288px ｜ 区域学习卡高度：252px
```

**288px 有多小**：画面是 4:3 的，按高度算只能渲染成 384×288，
而这一栏横向有 1100px 宽 —— 也就是说左右各空掉三百多像素。
所以这里每让出 100px 高度，画面宽度就多 133px，收益是平方级的。

### 40.2 五处压缩

| 位置 | 改法 | 为什么 |
|---|---|---|
| 左下角浮层工具条 | 「底图：…」那行**并进按钮行**（成为 WrapPanel 的第一个元素），内边距 8,4 → 7,3 | 这整块是**浮在画面上的**，少一行就少盖住约 26px 画面 |
| 右上角「等待触发」浮窗 | 内边距 14,10 → 10,7，字号 13/12 → 12/11，缩放条按钮 10,4 → 8,3 | 同样是浮层，缩小它 = 露出更多画面 |
| 框位条 | 外框 10,8 → 8,5；格子 92×56 → 82×44 | 它是画面下面真实占高的一行 |
| 区域学习卡 | 外框 10,8 → 8,5；按钮 12,5 → 9,3；缩略图 64×48 → 46×34；AI 自学框内 25 处字号/间距收紧 | 最大的一块，252px 里压掉 58px |
| 底部提示行 | 11px → 10px | 收尾 |

**注意**：只压了**次要文字**（辅助说明、Top3、按钮），
关键数值一个没动 —— 「本步已用时」18px、步骤进度 13px 加粗、识别结论 14px 加粗全部保持原样。

### 40.3 实测结果

```
相机画面区高度：288px → 356px      （+68px，这一版）
区域学习卡高度：252px → 194px      （-58px）
```

把上一版（底部历史表格 210 → 150）也算进去，相机展示位的变化是：

```
约 228px → 356px     （+128px，+56%）
```

换算成实际画面尺寸（4:3，等比缩放）：

```
304×228  →  475×356      面积约 1.6 倍
```

### 40.4 还能再大吗

现在的瓶颈已经不是"空白间距"，而是**功能本身占的高度**：区域学习卡 194px
（选框/教 OK/教 NG/开始识别 + 样本缩略图 + AI 自学一整块 + 提示）。

想再上一个台阶，只有两条路：

1. **把 AI 自学那块做成可折叠**（默认收起，用时点开）—— 能再让出约 70px；
2. **把区域学习整体挪到独立页**（首页只留相机 + 框位条）—— 能再让出约 190px。

这两条都改变了操作习惯，所以先不做，等现场确认。

---

## 四十一、底部历史条再压一轮（v1.9 · 2026-09-13）

现场要求："画圈的高度再压缩"（圈的是底部「历史数据」的标题行 + 表头行）。

### 41.1 改了什么

| 位置 | 原值 | 现值 |
|---|---|---|
| 底部那一条的固定高度 | 150px | **118px** |
| 卡片外边距 | 12,8,12,12 | 12,6,12,8 |
| 标题行内边距 | 14,10,14,8 | 12,6,12,4 |
| 「历史数据」标题 | 14px | 12px |
| 「（0 条）」计数 | 12px | 10px |
| 「导出 CSV」按钮 | 默认内边距 | 8,2 / 10px |
| **表格行高** | 32px | **24px** |
| **表头行高** | 34px | **22px** |
| 表格字号 | 12px | 11px |

行高和表头高是在共用的 `HistoryDataGrid` 样式里改的，
所以**历史查询页那张大表也一起变紧凑** —— 两处观感保持一致。

### 41.2 实测结果

```
相机画面区：356px → 388px
底部历史条：150px → 104px（118 的行高减去上下外边距）
```

累计（从 v1.7 之前算起）：

```
相机展示位：约 228px → 388px      +160px，+70%
```

### 41.3 还能再省 118px —— 但这次我不自作主张

底部这一条**和历史查询页里那张表是重复的**（历史查询页那张列还更全，
多了「用时(ms)」）。也就是说：把首页这一条整块去掉，相机位还能再涨 118px（约 +30%），
历史数据照样能查。

但这属于"改功能布局"，不是"压缩"，所以**先不做**：
现场说去掉，我再动手。

---

## 四十二、画面浮层工具条也压一档（v1.10 · 2026-09-13）

现场要求："同样高度缩小"（圈的是画面左下角那条浮层按钮：
标定 ROI / 导入现场图片 / 回到相机画面 / 同步为判定区域 / 框:显示）。

### 42.1 为什么这条特别值得压

它和别的卡片不一样：**它是浮在相机画面上的**。
它有多高，就直接盖住多少画面 —— 压它不会让别的卡片变大，但会直接多露出一条画面。

原来那一排每个按钮都是 `ToolButton` 样式的默认值（内边距 14,7 + 12px 字），
整行约 37px；`LIVE` 那个小徽标也是 12px 字。

### 42.2 改了什么

| 元素 | 原值 | 现值 |
|---|---|---|
| 整排按钮（5 个） | 内边距 14,7｜字号 12 | **内边距 9,3｜字号 10** |
| 按钮间距 | 6 | 5（右侧留白也收） |
| LIVE 徽标 | 内边距 7,3｜字号 12，圆点 8px | 内边距 6,2｜字号 10，圆点 7px |
| 底图来源标签 | 内边距 7,3｜字号 10 | 不变（上一版已收） |

**只改这一排**，没有动 `ToolButton` 样式本身 —— 那个样式被全项目几十个按钮共用，
改了会连带把所有页面的按钮都变小，那就不是现场要的效果了。

### 42.3 实测结果

```
画面浮层工具条高度：约 37px → 27px
```

也就是说，四角画面里**多露出约 10px 高的一条**（左右方向是整幅宽）。
同时按钮变窄了，窄屏上更不容易折行 —— 折行会让这条从一行变成两行，
那才是真正会吃掉画面的情况。

---

## 四十三、让内容铺满卡片内部（v2.1 · 2026-09-13）

现场要求："合理全部填充覆盖所在的卡片内部"。

### 43.1 先量清楚"是空着还是装不下"

这两种情况的改法完全相反，所以先在冒烟自检里加了三个测量点（都写在布局体检报告里）：

```
现场状态栏（左）：可用 624px，内容 392px  → 底部空着 232px
过程监测卡（右上）：可用 161px，内容 159px
当前工序卡（右下）：可用 413px，内容 329px → 底部空着  84px
```

### 43.2 三处改法

**① 左侧「现场状态栏」——让内容摊开铺满**

原来 9 个状态项是"从上往下堆"，堆完就没了，下面空两百多像素。
现在把这张卡的第一行从 `Auto` 改成 `*`，并把这 9 项放进 `UniformGrid`：
9 项按等分把上面的空间摊开、文字各自垂直居中，计时卡仍然贴在卡片底部。

**② 右下「当前工序」——让主角吃掉多余高度**

原来是 `StackPanel`：卡片再高也在底部留白。
现在改成 `Grid` 分行，**把「大号作业指引」那一行给 `*`** ——
多出来的高度全部被它吃掉，卡片被填满，而且"当前该做什么"这块变大，本来就该是这张卡的主角。

**③ 右上「过程监测」+ 另两张卡——高度自适应**

三张卡的内层都加了同一句：

```xml
MinHeight="{Binding ViewportHeight, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"
```

意思是：**内容比卡片矮 → 被拉到和卡片一样高，把内部填满；
内容比卡片高 → 老实出现滚动条，不丢东西**。
这样"填满"和"不丢内容"两个要求同时成立，不用二选一。

另外给大号作业指引加了 `MinHeight="56"`：万一窗口特别矮，
它也不会被压成一条缝把步骤名裁掉（宁可滚动，不可压扁）。

### 43.3 实测结果

```
现场状态栏：容器可用 624px，内容自然高 396px，实际占满 622px  ✔ 已铺满
过程监测卡：✔ 已铺满
当前工序卡：✔ 已铺满
```

布局体检的口径也一并改准了：现在同时报"内容自然高"和"实际占满"两个数，
于是能区分**装不下**（自然高 > 可用高）和**没铺满**（实际占满 < 可用高）——
这两个数在报告里出现的措辞也不一样，一眼能看出是哪种毛病。

---

## 四十四、权限真的上锁了（v2.3 · 2026-09-13）

现场反馈："这个权限没有设计好，没有密码等"（圈的是顶栏那个角色下拉框）。

### 44.1 原来到底有多松

两件事都挺严重：

1. **开机就是工程师**（`_currentUser = "工程师"`）—— 一打开软件就拥有全部权限；
2. **下拉框谁点谁切** —— 没有任何口令，那个锁图标纯粹是装饰。

### 44.2 现在的规则

| 规则 | 说明 |
|---|---|
| **开机默认操作员** | 想改配方/参数，自己去切角色、验口令 |
| **往上升要口令** | 操作员 → 技术员/工程师必须输对；**退回操作员不需要**（交还权限不该再拦一道） |
| **口令只存哈希** | PBKDF2-SHA256，随机盐，迭代 10 万次。存 `data\security\accounts.json` |
| **出厂默认口令** | 工程师 `888888`、技术员 `123456`、操作员不需要（生产岗位本来就该直接能用） |
| **一直提醒改默认口令** | 只要还有角色在用默认口令，设置页就一直挂提醒、登录框右下角也提示，**并且写进日志**——不给"忘了改"留余地 |
| **连续输错临时锁定** | 错 5 次锁 30 秒。**不做永久锁死是刻意的**：车间没有 IT 值守，永久锁死等于把整条线的运维堵死 |
| **空闲自动退回操作员** | 默认 15 分钟（可改，0 = 关闭）。工程师调完参数就走，界面不能还停在工程师 |
| **改口令的规矩** | 要验原口令；新口令 ≥4 位；两次要一致；不能和出厂默认口令相同 |
| **口令忘了怎么办** | 设置页「恢复默认口令」——但**必须先验证工程师口令**，否则这就是个一键夺权的后门 |
| **全程留痕** | 每次切换、失败、锁定、改密、自动降级都写 `data\security\yyyy-MM-dd.jsonl`，设置页显示最近 50 条 |

### 44.3 界面上的变化

* **顶栏**：角色下拉框换角色时弹口令框（回车即提交，口令错了带提示重试，最多 5 轮）；
* **登录框**：只做一件事——收一个口令串。校验全在 `AccessControlService` 里，
  将来要换指纹/刷卡，只换这个窗口；
* **系统设置页**：新增「账号与权限」卡：会话超时、操作员是否要口令、
  改口令（角色 + 原口令 + 新口令 + 确认）、恢复默认口令、权限操作记录。

### 44.4 口令框刻意不做数据绑定

`PasswordBox` **没有**绑到 ViewModel 的任何属性：口令由窗口代码取出来直接交给权限服务，
交完立刻 `Clear()`。绑到普通字符串属性意味着口令会一直留在内存的托管堆里，
可能被日志、dump 或异常信息带出去——这类细节正是"权限没设计好"的一半。

### 44.5 自检新增 9 条（AC-01 ~ AC-09）

```
AC-01 口令只存哈希：同一口令两次结果不同（随机盐）、错口令与坏字符串都拒绝
AC-02 升级必须口令：错口令被拒且角色不变，对的口令才切到工程师
AC-03 降级不需要口令
AC-04 连续输错临时锁定，锁定期间正确口令也拒绝
AC-05 改口令：原口令错/太短/两次不一致/与默认相同 全部拒绝；改成功后旧口令失效
AC-06 恢复默认口令必须验工程师口令
AC-07 会话超时：没到点不降级，超时自动退回操作员
AC-08 默认口令提醒：有默认口令就提醒，全改完提醒消失
AC-09 改完口令后仍能正常登录
```

另外，**界面冒烟自检现在会把登录框也真开一次**并写进报告 ——
它是权限的唯一入口，坏了现场就永远进不了工程师，而这条路径平时走不到、人工验收很容易漏。

### 44.6 现场要做的第一件事

进「系统设置 → 账号与权限」，把**工程师和技术员的出厂默认口令改掉**。
在那之前，设置页的黄色提醒一直都在。

---

## 四十五、拿笔流程跑不起来：两个真实问题的修正（v2.4 · 2026-09-13）

现场反馈：

> 1. 视频能上传，就是导入后，不能暂停视频。
> 2. 标记了三个框，OK/NG 标识了，但是没法跑一套流程。

### 45.1 问题一：导入后视频自己就播起来了

先做了回归测试 **UI-21**（自检里真打开一段录像，量位置变化）——结论是
**播放 / 暂停 / 重播这条链路本身是好的**：

```
播放 0.49s→1.10s（前进 0.61s）
暂停后 1.10s→1.10s（变化 0.00s）
重播后 0.21s
```

那"不能暂停"是怎么回事？看代码就明白了：`导入录像` 之后紧接着
`StartGrabbingAsync()` —— **它会自动开始播放**。于是现场的真实体验是：

- 一导入画面就在动，人根本没法从容地画框、教 OK/NG；
- 31 秒一过录像播到结尾停住，更像"卡住了"。

**改法**：导入后**先暂停在第 1 帧**，状态栏明确写"已暂停在第 1 帧：先画框、教 OK/NG"；
等点「▶ 播放录像」，或者点「开始识别」时**自动开始播**（识别本来就需要画面在动）。
播到结尾后再点播放会自动从头开始，不用先去点「重播」。

### 45.2 问题二：默认规则把"拿走"类工序判反了

这一条是根因，而且是个设计缺口：

原来框的规则只有一种 —— **框里有东西 = 这一步做到了**（放料、装配类工序）。
而现场试的流程是 **"从左上拿一支笔 → 从右上拿一支笔 → 放到中间"**，
这是**拿走**型工序，规则正好相反。

于是在默认规则下：

1. 一开始两支笔都在 → 三个框全判"完成" → **流程瞬间走完**；
2. 然后人真的去拿笔 → 框变空 → 被判成 NG → **报警**。

现场的感受就是"没法跑一套流程"，完全对得上。

**改法**：给每个框加一个**完成条件**：

| 完成条件 | 适用工序 | 判定 |
|---|---|---|
| **有东西 = 完成**（默认） | 放料 / 装配 / 插入 | 框里出现零件 → 这一步完成 |
| **被拿走 = 完成** | 取件 / 拿工具 / 拿笔 | 框里的东西消失 → 这一步完成 |

用起来：**在框位格子上右键 →「切换完成条件（有东西 ⇄ 被拿走）」**。
切换后格子的颜色含义统一成"这一步做到没有"（绿=完成、红=没完成、橙=拿不准），
格子上的字也从 `OK/NG` 改成 `完成/未完成`，避免两种口径混着看。

这样一来，"第三次没拿右上角的笔"就**一定会被抓到**：
那个框的完成条件是"被拿走"，笔还在 → 这一步永远不完成 → 流程停在该步、报 NG、锁线。

### 45.3 自检新增 2 条（UI-21 / UI-22）

```
UI-21 录像当相机：能播放、能暂停（暂停后位置不再前进）、能重播回开头
UI-22 拿走型工序：切成「被拿走」后，笔还在=未完成、笔拿走=完成、拿不准=不算；默认规则正好相反
```

UI-21 还顺带把两次踩到的坑固化成经验（写在用例注释里）：

* 录像用的是 WPF `MediaPlayer`，事件只投递到创建它的线程的 Dispatcher 上 ——
  自检默认跑在线程池线程（没有消息循环），所以必须单起一个**带消息泵的 STA 线程**，
  否则 `MediaOpened` 永远不触发，测出来的是"打不开"而不是真实行为；
* 等它连接时不能 `GetAwaiter().GetResult()`（会把线程堵死、消息循环不转），
  要**边泵消息边等**；
* 那个线程还得装上 `DispatcherSynchronizationContext`，否则 `await` 之后的续体会跳到线程池，
  而 `MediaPlayer` 是 DispatcherObject，跨线程访问直接抛异常。

另外为了让这条用例永远能跑，包里带了一段 **3 秒、15KB 的测试录像**
（`Assets\test-clip.mp4`），不再依赖现场视频。

---

## 四十六、"标定 ROI 不能用" —— 灰按钮没解释自己（v2.5 · 2026-09-13）

现场反馈：

> 1. 视频可以单击启动，停止按钮
> 2. 标定 ROI，不能用，什么情况

### 46.1 第 2 条是我的锅：权限上了，提示没上

查代码一句话就定位了：

```csharp
ToggleRoiEditCommand = new RelayCommand(ToggleRoiEdit, () => CanTuneRecipe);
public bool CanTuneRecipe => !IsOperator;
```

而 **v2.3 刚好把开机默认角色从"工程师"改成了"操作员"**。
两件事凑一起的结果就是：按钮变灰、点了没反应，**界面上没有任何说明**。
现场的原话是"标定 ROI 不能用，什么情况"——这确实是个故障，是我把权限做得"只有锁、没有说明"。

**改法**：

* 顶部工具条上多一条**黄色提示**，只要当前角色不够就直接写清楚：

  > 🔒 当前是「操作员」：标定 ROI / 改参数需要「技术员」或「工程师」权限
  > —— 点右上角 🔑 切换（工程师默认口令 888888）

* 「标定 ROI」按钮加了悬停说明（"需要技术员或工程师权限，操作员用不了"）；
* 切到技术员/工程师之后，这条提示自动消失。

**自检新增 AC-10**，把这条原则固化下来：

```
AC-10 权限不够时按钮必须解释自己：操作员点不了「标定 ROI」
      但界面写清了原因和切换办法；切到工程师后按钮可用、提示消失
```

### 46.2 第 1 条：加了一键「■ 停止」

录像工具栏原来是「▶ 播放 / ⏸ 暂停」（同一个按钮切换）、「⟲ 从头再播」、「⏹ 停止分析」。
现在补上一个**独立的「■ 停止」**：

* 点一下 = **停止播放并倒回第 1 帧**（录像仍然是当前相机源，不会关掉）；
* 再点「▶ 播放」就从第一帧开始 —— 现场要的是"点一下停住、再点一下从头来"，
  不用先去够「从头再播」。

和「⏹ 停止分析」的区别：**停止分析会断开录像**（回到没有画面的状态），
**停止只是停住播放**（画面还在，可以继续画框/教样本）。

所以现在录像这一排是：`▶ 播放/⏸ 暂停` ｜ `■ 停止（回开头）` ｜ `⟲ 从头再播` ｜ `⏹ 停止分析（断开）`，
四个都是一次点击，不用记先后顺序。

---

## 三十四、代码上传到 GitHub（v1.0.30 · 2026-09-12）

目标仓库：<https://github.com/qian1586/-AI-SOP>

### 34.1 已经准备好的东西

| 内容 | 说明 |
|---|---|
| **本地 git 仓库** | 已在本目录初始化（分支 `main`），第一次提交：`V1.0 初始版本：追觅洗地机AI-SOP监测系统（源码 + 部署脚本 + 文档）` |
| **153 个文件** | 源码 122 · 脚本 7 · 已编译的可运行版本 9 · 文档与说明若干 |
| **`.gitignore`** | 已排除 `bin/`、`obj/`（编译中间产物，每次重建都变）、日志与运行数据 —— 仓库保持小而干净 |
| **一键上传** | 双击 `上传到GitHub.cmd`：自动把版本号 +0.1（V1.0 → V1.1 → V1.2 …）→ 提交 → 推送 |

### 34.2 你要做的（只有第一次多一步）

1. **第一次**：双击 `上传到GitHub.cmd`
   - 如果还没登录过 GitHub，脚本会**自动引导登录**（浏览器打开一个页面，输入页面上的 8 位代码即可）；
   - 登录一次之后长期有效，以后直接双击就上传。
2. 如果远端仓库里已经有内容（比如建仓库时勾了 "Add a README"），脚本会**自动合并后再推一次**，不用你处理冲突。

### 34.3 想写备注就这样

```
scripts\upload-to-github.ps1 -Note "修好了画框和相似度"
```

不写备注也行，提交信息会是 `V1.1 自动上传` 这种格式 —— 版本号每次自动递增，
`git log` 一眼能看出改了几次、每次改了啥。

### 34.4 为什么是"我准备好、你双击上传"

沙箱**能连上 github.com**（实测 Python 访问 GitHub API 返回 200），但有两条硬限制：

1. 这台机器上**没有安装 Git for Windows**（`C:\Program Files\Git` 不存在），
   沙箱自带的那个 git 是精简运行时，缺少 `git-remote-https`，任何 https 推送都会报
   `git: 'remote-https' is not a git command`；
2. **没有任何 GitHub 凭据**：没有 `.git-credentials`、没有 netrc、没有 ssh key，
   `gh` 也从未登录过 —— 也就是说"替你把文件传上去"缺的是**账号授权**，不是网络。

所以最终方案改成了**不依赖 git 的上传器**（走 GitHub 官方 REST API），
见下一节。授权仍然必须由你点一次（GitHub 的规矩，谁也不能代替账号本人授权）。

---

## 三十五、AI 自主学习：让识别率自己往上涨（v1.0.31 · 2026-09-13）

问题原话："你写的这个软件有 AI 自主学习提升识别率的功能吗，没有的话需要增加自学能力。"

**原来的情况**：只有"教示识别"——人工对着画面点「记 OK」「记 NG」，系统靠最近邻比对，
样本不再增长。它**不会自己变准**：教多少就是多少，现场换个光照、换个摆放角度，
识别率就往下掉，得人工再补教一遍。

**现在加了什么**（Core/Services/SelfLearning.cs + 界面上一整条"AI 自主学习"）：

### 35.1 两条腿走路

| 学习方式 | 什么时候发生 | 谁出的标签 |
|---|---|---|
| **自动收录** | 识别开着、系统判某个框 OK、把握 ≥ 门槛、这一步又确实正常完成 | 系统自己（只在有把握时） |
| **人工纠错** | 人发现判错了，点「其实是对的 / 其实是错的」（或框位右键"标记误判"） | 人（最高优先级） |

自动收录对应基恩士 Auto-Teach 的"只跑良品自学习"：正常生产占掉绝大部分时间，
等于把"人再教一遍"这件事自动化了。人工纠错补的则是模型最薄弱的那类画面 ——
**它专门学系统刚犯错的地方，所以提升识别率最直接**。

### 35.2 五条护栏（这一节才是重点）

一个不加限制的自学习系统会在现场悄悄跑偏，而且跑偏了没人看得出来。所以：

1. **拿不准就不学**：系统自己判「无法判定」、或置信度低于门槛（默认 90%）的帧，一条都不收。
2. **NG 默认不自动学**：万一系统误报一次 NG 又被自己学进去，以后看到同样的正常画面
   就会更坚信"这是违规"——越学越错。所以 NG 必须人工确认；开关在界面上，默认关。
3. **几乎一样的只强化不新增**：相似度 ≥ 98.5% 视为同一情况，只把那条的"命中次数" +1。
   不这么做，对着一个静止画面跑一分钟就能把样本库灌满，而且全是同一张，识别反而变差。
4. **只淘汰自学的**：人工教的、人工纠错的样本**永远保留**；数量到顶时只从自学样本里挑，
   并且优先淘汰**被见到次数最少**的那条（`Hits` 最小，并列时最早的先走）。
   直觉：一个画面反复出现说明它是常态，只见过一次的样本最可能是偶发画面。
   这个判据还有一个工程上的好处 —— 它是 O(n) 的（见下面 37.3 的实测）。
5. **数量封顶**：每个框、每类最多 40 条（可调），样本库不会无限长大（对应 7×24 连跑的内存要求）。

### 35.3 界面上怎么用（在"区域学习"卡片下面）

| 界面元素 | 干什么 |
|---|---|
| **🤖 自学：开 / 关** | 一键开关。关掉之后行为和以前完全一样（已收的样本仍参与识别） |
| **自动收 OK** | 勾上 = 有把握的良品帧自动进样本库（默认开） |
| **自动收 NG（慎用）** | 默认关。现场确认过"这批 NG 是真的"再打开 |
| **把握门槛 / 每类上限** | 两个数字框，直接改，改完立即生效并写进 `appsettings.json` |
| **↩ 撤销上次** | 撤掉刚学进来的那一条（只撤自学的） |
| **🧹 清空自学** | 换产品/换工装后清掉自学样本重新学；人工样本保留 |
| **📌 固化** | 把当前自学样本升级成人工样本 —— 以后系统再也不动它们 |
| **📂 自学账本** | 打开 `data\learning\`：什么时候学了什么、为什么没学，全都在 |
| **✅ 其实是对的 / ❌ 其实是错的** | 对着当前选中的框做人工纠错（也可在框位格子上右键选） |

界面上还会自己说一句**该教哪个框**：
"⚠️ 第 3 个框已经连续 120 帧拿不准 —— 建议对着它补 2~3 条「记 OK」「记 NG」"。
这句话是自检/调参最有用的输出，它把"识别率不够"翻译成了现场能执行的动作。

### 35.4 数据落在哪

| 文件 | 内容 |
|---|---|
| `data\roi-samples.json` | 样本库。每条多了 `Source`（Manual/Auto/Corrected）、`Hits`、`Confidence`、`Note` 四个字段 |
| `data\learning\yyyy-MM-dd.jsonl` | 自学账本：一天一个文件，追加写（断电最多丢最后一行） |

老版本存的样本文件**不用改**：没有 `Source` 字段就按 `Manual` 处理 ——
语义正好也对（以前能存在的样本都是人教的）。

### 35.5 为什么不影响判定

自学只**收样本**、不改判定：它跑在"判定之后"，学到的样本下一帧才参与比对。
所以最坏情况也只是样本库多了几条，随时可以撤销/清空 —— 不会出现"软件突然乱判"。

### 35.6 自检新增 8 条（每次重建自动跑）

`AI-01 ~ AI-08`：三条护栏各拦一次、有把握才学、重复只强化、NG 默认不学、
封顶只淘汰自学样本、人工样本满时明确拒绝、撤销与固化、人工纠错优先级最高、
以及一条真实链路（自学一条之后"有东西=OK / 空框=NG"照旧判得对）。

看结果：重建窗口的日志，或 `app\data\selftest\report.txt`。
