# 使用说明（现场怎么用）

> 面向工位上的操作员和班长。工程师配置那部分在最后一节。

---

## 一、界面一眼看懂

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：启动监测 / 重置流程 / 大屏 / 导入录像 ｜ 操作员 ｜ 🔑角色 │
├───────────────┬──────────┬────────────────────────────────────┤
│               │ 现场状态  │  过程监测 · 手部动作（一键演示）    │
│   相机画面     │ 工单号    ├────────────────────────────────────┤
│  （绿框=做到） │ 产品型号  │  当前工序（当前该做哪一步、判定）    │
│  手部骨架      │ 步骤进度  │  实时统计 OK/NG/合格率/完成件数      │
│               │ 合规率    │  模拟人工动作（测试用）              │
│               │ 节拍时间  │  过程数据                          │
├───────────────┴──────────┴────────────────────────────────────┤
│ 框位（一格=一个框）｜ 区域学习：选框→记 OK/记 NG→开始识别 ｜ AI 自学 │
├──────────────────────────────────────────────────────────────┤
│ 底部：工单状态行 ｜ 历史数据表（最近的检测记录）                 │
└──────────────────────────────────────────────────────────────┘
```

颜色统一是这一个意思：**绿 = 这一步做到了、红 = 没做到、橙 = 拿不准**。

---

## 二、日常生产怎么用（操作员）

1. **双击 `2-启动VisionForge.cmd`** 打开软件；
2. 点 **「启动监测」** —— 相机连上就开始出画面；
3. 按真实作业顺序干活。每做完一道工序：
   * 对应的框变**绿**、步骤条往前走一格；
   * 做错了（跳步 / 漏做 / 顺序颠倒）→ 框变红、弹出报警、**压合机构会被锁住**；
4. 一件做完 → 计数 +1，流程自动回到第 1 步；
5. 报警要消音，点界面上的「消音」（或按报警面板上的按钮）。

**注意**：启动后角色是**操作员**（这是有意的，防止开机就带着全部权限）。
操作员只能作业 —— 改配方、标定 ROI、调参数都需要切换角色（见第五节）。

---

## 三、没有相机时怎么试（导入录像）

顶栏「📹 导入录像」→ 选一段工位实拍视频 → 它会**暂停在第一帧**，
方便你先画框、教样本；准备好了点「▶ 播放」或者直接点「开始识别」（会自动播）。

录像这一排按钮：

| 按钮 | 作用 |
|---|---|
| ▶ 播放 / ⏸ 暂停 | 播放 ⇄ 停在当前帧 |
| ■ 停止 | 停住并回到第一帧（录像仍然当相机用） |
| ⟲ 从头再播 | 从第一帧开始播 |
| ⏹ 停止分析 | 断开录像，回到没有画面的状态 |

> 录像必须是 **H.264 编码**。手机/微信拍的常常是 H.265，
> 那种在这台机器上会打不开（报"视频编解码器未安装"），先用工具转成 H.264。

---

## 四、教它认东西（最常用的功能）

### 4.1 画框

1. 切到工程师角色（见第五节）；
2. 点「🎯 标定 ROI」进入标定模式（**操作员用不了这个按钮**，按钮会变灰并给出提示）；
3. 在画面上**按住左键拖拽**画出要监测的位置（零件的凹槽、取料位、装配位…）；
4. 画错了：右键那个框 → 删除，或右键改名称/参数。

**一个框 = 一道工序**：第 1 个框对应第 1 步。画几个框，工序就有几道。

### 4.2 教 OK / NG（判断"有没有"）

选中一个框 → 零件放好时点 **「✔ 记 OK」**、零件拿走时点 **「✘ 记 NG」**，
每类各教 3~5 条（换个角度、换个光照各教一条，识别更稳）。

### 4.3 教类别（判断"是不是那个东西"）—— 推荐

比"有没有"更进一步：

1. 在「类别名」里写名字（例：`导光柱`）；
2. 把那一类东西放上去，点 **「＋ 记这一类」**（同一个框可以教好几类）；
3. 在「这一步应该是」写上这一步完成时应该是什么（可多个，用、分隔）；
4. 之后系统会分三种结论：
   * 认出就是它 → **通过**；
   * 认出是别的类别 → **装错**；
   * 框里空的 → **漏装**。

### 4.4 完成条件（"拿走"类工序必须改）

放料/装配类工序默认是"框里有东西 = 这一步完成"；
但**取件、拿工具**这类工序正相反。在框位上**右键 → 「切换完成条件」**即可。

> 不改这条会出怪事：一开始东西都在，三个框全判"完成"、流程瞬间走完。
> **教法不变**（有东西记 OK、拿走记 NG），只切完成条件。

---

## 五、切换角色（要口令）

顶栏那个 **🔑** 下拉框：

| 角色 | 出厂默认口令 | 能做什么 |
|---|---|---|
| 操作员 | 不需要 | 只作业：启动监测 / 触发 / 消音 |
| 技术员 | `123456` | 加：调参数、标定 ROI、人工放行 |
| 工程师 | `888888` | 全部：配方增删改、系统设置、权限管理 |

**往上升要口令，退回操作员不用。**
为了安全，空闲 15 分钟会自动退回操作员（可在系统设置里改或关掉）。

**装好后第一件事**：进「系统设置 → 账号与权限」把两个默认口令改掉。

---

## 六、现场状态与手部动作（新功能）

画面左上角会显示手部识别的状态：

```
手部关键点：21 关节 · 置信度 83% · 31ms
手部位置：第 2 个框（已停留 480ms）
⚠ 错序：手先去了第 2 步「套外壳」，当前应该做第 1 步
手的动作轨迹：第1框→第2框
```

* **绿线是骨架、红点是关节**（置信度低的点会自动变暗）；
* 手在某个框里**停留够时间**才算"这里做了一个动作"，手从框上划过去不算；
* 手先去了后面才该做的那一步 → 判**错序**（这条以前只能靠零件判定发现）。

---

## 七、回看与追溯

| 想看什么 | 去哪 |
|---|---|
| 最近的检测记录 | 首页底部「历史数据」表 |
| 按日期/批次筛选、导出 CSV | 顶栏「历史查询」页 |
| 每条动作花了多久、比上次快还是慢 | 右侧「⏱ 动作时间回看」 |
| 违规当时的画面 | 异常列表里点那条（或看 `data\ng-images`） |
| 它自己学了什么 | 区域学习卡里的「📂 自学账本」 |

---

## 八、AI 自主学习（边生产边变准）

在「区域学习」卡片下面有一整条「🤖 AI 自主学习」：

* **自动收 OK**（默认开）：系统有把握、这一步又确实完成了 → 自动把这一帧收成样本；
* **人工纠错**：它判错了，你点「✅ 其实是对的」/「❌ 其实是错的」，当场纠正；
* **NG 默认不自动学**（防误报被学进去，越学越错）；
* **↩ 撤销上次 / 🧹 清空自学 / 📌 固化**：学歪了随时退回去。

---

## 九、工程师配置清单（装机时走一遍）

1. 系统设置 → 相机 / PLC / 工位联网 / MES 对接，把地址填对，保存；
2. 标定 ROI：画框 → 每框教 OK/NG 或教类别 → 设"完成条件"与"这一步应该是"；
3. 时间设置：每道工序的标准工时（用来判"慢了"）；
4. 账号与权限：改掉默认口令；
5. 跑一遍「一键演示」确认流程通，再看底部状态条的合规率；
6. 正式生产前建议按 `方向对齐说明.md` 里的建议录一段标准作业做回归验证。
