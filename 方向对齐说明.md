# 方向对齐说明：从"防漏装"走到"AI 行为识别 SOP 检测"

> 起因：现场看了 CCD.mp4 那份拆解后的判断 ——
> "我们做的这个系统，方向跑偏了，目前的形势变成了 CCD 产品缺失检测，应该做成上面文章的效果。"
>
> 这份文件把**现状、差距、路线**三件事写清楚，避免后面再跑偏。

---

## 一、现在的系统到底是什么

一句话：**它是"每个框里有没有东西"的检测器 + 一套工序状态机**。

| 已有能力 | 说明 |
|---|---|
| 区域二值判定 | 每个框教 OK（有东西）/ NG（空），最近邻比对，低于阈值判"无法判定" |
| 工序顺序 | 跳步 / 错序 / 超时 / 重复触发，判违规并锁流程 |
| 完成条件 | 有东西=完成（放料类）/ 被拿走=完成（取件类） |
| 置信度门槛 | 相似度低于阈值不硬猜（可调，默认 75%） |
| 报警与联动 | 违规码写 PLC、声光报警、停线锁压合 |
| 留痕 | NG 图、证据帧、动作计时、历史查询、CSV、MES 上报 |
| 自学 | 有把握时自动收样本、人工纠错、上限与回滚 |

**所以它不是"什么都没有"，而是分辨率不够**：它只知道"有没有"，不知道"是什么"。

---

## 二、跟目标（CCD.mp4 拆解）的差距

| 目标能力 | 现在 | 差距性质 |
|---|---|---|
| 放入功能件 / 套外壳 / 套小盖 / 装导光柱 / 放面盖 —— **认出是哪一类** | ❌ 只判有/无 | **已开工**（v2.6 的"按类别判"，见第三节） |
| 装错 vs 装漏 分开报 | ❌ 只报 NG | ✅ 已随"按类别判"一起解决 |
| 手部骨骼 21 关节 + 轨迹判断在哪个工位 | ❌ 无 | **需要模型**（见第四节） |
| 置信度数字直接显示在框上（0.98 绿 / 0.55 红） | 🔶 有阈值与变色，数字只显示在面板 | 小改，1 天内 |
| 顺序校验 / 跳步倒序 | ✅ 已有 | — |
| 质量项：螺丝漏装 / 标签缺失 / 歪斜 / 模糊 / 压不平 | 🔶 可用"多类别框"表达一部分；歪斜/模糊/高度需要专门的算法 | 分批做 |
| 压合机构联动 | ✅ 已有（PLC 规则码 + 报警） | — |
| 视频帧 + 置信度 + 判定的完整追溯 | ✅ 已有（历史 + NG 图 + 证据帧） | — |

**结论：偏的不是架构，是"识别粒度"。** 状态机、报警、联动、留痕这些最难做的部分已经在位，
缺的是把"有没有"升级成"是什么 + 手在做什么"。

---

## 三、已经做的第一步（v2.6）：按类别判

把"框里有没有东西"升级成"框里是哪一类"，**不需要任何模型、不需要训练数据**：

1. 在「类别名」里写一个名字（功能件 / 外壳 / 小盖 / 导光柱 / 面盖 / 空 …）；
2. 把那一类东西放上去，点「＋ 记这一类」——**同一个框可以教好几类**；
3. 在「这一步应该是」里写这一步完成时应该是什么（可多个，用、分隔）；
4. 判定结果自动分三种：

| 识别结果 | 判定 | 现场看到的字 |
|---|---|---|
| 就是这个类别 | ✅ 通过 | `导光柱（正确）` |
| 是别的类别 | ❌ **装错** | `装错（识别为「外壳」，应该是「导光柱」）` |
| 空的 | ❌ **漏装** | `漏装（框里什么也没有，应该是「导光柱」）` |

自检 UI-23 已经覆盖这三种结果，报告里能看到原话。

> 这一步是把 SOP 六道工序（放功能件 → 套外壳 → 套小盖 → 装导光柱 → 放面盖 → 压合）
> 从"每步有没有做"变成"每步做的是不是那个东西"的钥匙。

---

## 四、下一步：手部骨骼关键点（需要你拍板）

目标里"人手骨骼 + 轨迹 + 判断在哪个工位"是**必须上模型**的部分，绕不过去：

| 需要什么 | 代价 |
|---|---|
| 推理运行时 `onnxruntime` | 加一个 NuGet 依赖，约 50MB，跟着程序一起发 |
| 手部关键点模型（RTMPose-hand / MediaPipe hand landmarks 那类） | 模型文件 10~30MB，需要联网下 |
| 接进现有架构 | 代码里已经有 `IPoseSource` 接口和"模拟姿态源"，把真模型挂上去即可，界面不用动 |

**为什么我没有直接做**：这是给交付程序加两个体积不小的依赖，
而且换模型/换版本会影响现场稳定性 —— 这类事我不能不打招呼就动。

**你说一句"上模型"，我就按这个顺序做**：接 onnxruntime → 接模型 → 把 21 点画到画面上 →
用关键点算"手在哪个区域" → 与"零件类别"一起进 SOP 判定。

另外要提前说清楚一句**期望管理**：文章里那种"每个动作 0.9x 置信度"的效果，
背后是**拿现场良品/不良样本训练出来的专用模型**。模型这一层做不到"零样本开箱即用"：
相机装好之后，每个工位至少要录一段标准作业视频用来标定与验证
（这一步在现场做，不是我在电脑前能替代的）。

---

## 五、三阶段路线（建议）

| 阶段 | 内容 | 依赖 | 状态 |
|---|---|---|---|
| **一** | 按类别判（装对/装错/漏装）+ 置信度数字上框 | 无（纯软件） | 类别判 ✅ / 数字上框 ⏳ |
| **二** | 手部骨骼 21 点 + 手在哪个工位 + 轨迹 | onnxruntime + 模型 | 待你拍板 |
| **三** | 质量项：标签歪斜/模糊、压平、螺丝孔位 | 专用算法 + 现场样本 | 待排期 |

阶段一做完，你那段 CCD.mp4 里的**六道工序 + 装错/漏装报警**就能跑出来；
阶段二做完，才有"手指关节连线 + 红点"那套画面。

---

## 六、现场需要配合的事

1. **相机与治具固定**：顶部垂直打光，手不要挡住治具（否则模型再好也白搭）；
2. **每个框多教几类**：功能件/外壳/小盖/导光柱/面盖各教几条，比只教"有没有"稳得多；
3. **录一段标准作业视频**（含至少一次故意装错的），我可以用它回归验证；
4. 阈值初期设 0.85，稳定后提到 0.92 以上（可在界面上调）。
