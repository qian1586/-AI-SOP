# 安装说明（换一台电脑装）

> 这份包是**免安装**的：整个文件夹拷过去就能用。
> 只有一件事需要装 —— .NET 8 桌面运行时（微软的，包里已经带上了安装文件）。

---

## 一、对电脑的要求

| 项目 | 要求 | 说明 |
|---|---|---|
| 操作系统 | **Windows 10 1809 以上** 或 Windows 11（64 位） | 工控机常见的是 Win10 LTSC，可以 |
| 运行环境 | **.NET 8 桌面运行时** | 包里自带安装文件，**不需要联网** |
| 内存 | 建议 8GB 以上 | 手部模型常驻内存约 300MB |
| 硬盘 | 至少 2GB 空闲 | 程序 + 模型约 100MB，历史数据会慢慢长大 |
| 显示器 | 1920×1080 最合适，最小 1366×768 | 界面按 1920×1080 调过，更小的屏会滚动 |
| 相机 | **彩色**工业相机（USB3 / GigE）或一段录像 | **手部关键点必须彩色**：实测灰度画面模型认不出（彩色置信度 83%，灰度 0.4%） |
| PLC | 支持 Modbus TCP 的即可（也可以先用模拟） | 没有 PLC 也能跑，报警只显示在界面上 |

---

## 二、复制过去、装起来（三步）

### 第 1 步：整包拷贝

把 **整个 `VisionForge-便携版-v2.8` 文件夹** 拷到新电脑上（U 盘、共享盘都行）。

> ⚠️ 一定要**整个文件夹一起拷**，不要只拷 `app` ——
> `runtime`（运行库安装包）和文档都在外面。
> 建议放在 **D 盘**（比如 `D:\VisionForge-便携版-v2.8`），系统盘满的时候工控机会出怪问题。

### 第 2 步：装运行环境

双击 **`1-安装运行环境.cmd`**：

* 它会先检查本机有没有 .NET 8 桌面运行时；
* 有 → 提示"不用再装"，直接结束；
* 没有 → 自动静默安装包里带的那个（约 1 分钟）。

如果公司电脑禁止静默安装，它会退回到**图形安装界面**，按提示点"安装"即可。

### 第 3 步：启动

双击 **`2-启动VisionForge.cmd`**。

想确认环境是否齐全，可以先双击 **`3-环境检查.cmd`**，它会告诉你：
系统版本、运行库装没装、程序文件齐不齐、磁盘剩多少。

---

## 三、第一次启动会看到什么

**开箱状态是没有硬件的**：默认挂一台"模拟相机"和一台"模拟 PLC"，
所以界面能起来、能点，只是没有真实画面。这是故意的 ——
不接硬件也能先把界面、流程、配方都试一遍。

要接真实设备，进「系统设置」改这几处：

| 要改什么 | 在哪改 |
|---|---|
| 相机（品牌 / IP / 序列号） | 系统设置 → 相机 |
| PLC（品牌 / IP / 端口） | 系统设置 → 通讯 |
| 本工位编号 / 汇总终端地址 | 系统设置 → 工位联网 |
| MES 接口地址 / 令牌 | 系统设置 → MES 对接 |

改完点「保存设置」。配置文件的真实位置在：

```
<便携包>\app\config\appsettings.json
```

**这个文件可以直接用记事本改**（现场工程师不用理解代码就能改 IP）。

---

## 四、换电脑时要注意的三件事

1. **权限口令不会跟着走**。新电脑上是出厂默认口令：
   工程师 `888888`、技术员 `123456`。
   **装好第一件事就是进「系统设置 → 账号与权限」把这两个改掉。**

2. **要接着用老工位的数据**，把老电脑上的 `app\data` 整个拷过来覆盖即可
   （里面有配方、教示样本、历史记录、自学账本、权限审计）。

3. **相机和 PLC 的 IP 通常要重设** —— 新工位的地址大概率不一样。

---

## 五、出问题怎么查

| 现象 | 先看哪里 |
|---|---|
| 双击没反应 / 闪一下就没了 | 先跑 `3-环境检查.cmd`，多半是少了运行库 |
| 界面能开但没画面 | 系统设置里的相机配置；或先「导入录像」用录像试 |
| 手部骨架不显示 | 相机是不是**黑白**的（手部模型只认彩色）；左上角状态条会写明原因 |
| 想确认程序本身有没有问题 | 命令行执行 `app\VisionForge.Main.exe --selftest`（95 项自检，会写报告） |

自检报告位置：

```
<便携包>\app\data\selftest\report.txt
```

界面出不来时（这就是"打不开"），执行：

```
app\VisionForge.Main.exe --smoketest
```

它会真开一次主窗口并把结果写进 `app\data\smoketest\report.txt`。
